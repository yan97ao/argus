# 每日更新报告（2026-01-17）

## vllm-project/vllm

| 提交时间 | 作者 | 提交信息 |
|----------|------|----------|
| 2026-01-17 21:45:30 | Mritunjay Kumar Sharma | [CI/Build][Docker] Add centralized version manifest for Docker builds (#31492) |
| 2026-01-17 20:18:30 | Guofang.Tang | [Misc] Fix typo: seperator -> separator in flashmla_sparse.py (#32411) |
| 2026-01-17 17:33:05 | Kim Hee Su | [Model] Molmo2: Enable quantized weight mapping for vision backbone (#32385) |
| 2026-01-17 14:12:55 | Paul Pak | [Models] Lfm2Moe: minor name changes for resolving lora conflicts (#29063) |
| 2026-01-17 12:52:33 | Shengqi Chen | [CI] Implement uploading to PyPI and GitHub in the release pipeline, enable release image building for CUDA 13.0 (#31032) |
| 2026-01-17 12:42:39 | Matthew Bonanni | Revert "[Attention][MLA] Make `FLASHINFER_MLA` the default MLA backen… (#32484) |
| 2026-01-17 11:23:45 | vanshil shah | apply _validate_input to MistralTokenizer token-id chat prompts (#32448) |
| 2026-01-17 10:35:49 | Simon Mo | [Docs][Governance] Add @robertshaw2-redhat to lead maintainers group (#32498) |
| 2026-01-17 07:29:20 | Chenyaaang | [TPU][Core] Enable Pipeline Parallelism on TPU backend (#28506) |
| 2026-01-17 05:27:16 | Lucas Wilkinson | [CI] Fix OOM in Hopper Fusion E2E Tests (H100) (#32489) |
| 2026-01-17 05:14:40 | Andrew Xia | [responsesAPI] allow tuning include_stop_str_in_output (#32383) |
| 2026-01-17 02:38:07 | Xin Yang | [LoRA] Update LoRA expand kernel heuristic (#32425) |
| 2026-01-17 01:45:04 | Hashem Hashemi | Atomics Reduce Counting Optimization for SplitK Skinny GEMMs. (#29843) |
| 2026-01-17 01:18:05 | Wentao Ye | [CI] Update deepgemm to newer version (#32479) |

### 📊 统计摘要
> 本日共 14 个提交 | 🔴高 1 | 🟡中 4 | 🟢低 9
## 📋 目录

- [vllm-project/vllm](#vllm-project-vllm)
  - [📊 统计摘要](#-统计摘要)
  - [🔴 高重要度变更 (1)](#-🔴-高重要度变更-1)
    - [Atomics Reduce Counting Optimization for SplitK Skinny GE...](#7a10304)
  - [🟡 中重要度变更 (4)](#-🟡-中重要度变更-4)
    - [[CI/Build][Docker] Add centralized version manifest for D...](#9e078d0)
    - [[CI] Implement uploading to PyPI and GitHub in the releas...](#8e61425)
    - [Revert "[Attention][MLA] Make `FLASHINFER_MLA` the defaul...](#2e7c89e)
    - [apply _validate_input to MistralTokenizer token-id chat p...](#037a648)
  - [🟢 低重要度变更 (9)](#-🟢-低重要度变更-9)
    - [[Misc] Fix typo: seperator -> separator in flashmla_spars...](#2b99f21)
    - [[Model] Molmo2: Enable quantized weight mapping for visio...](#1646fea)
    - [[Models] Lfm2Moe: minor name changes for resolving lora c...](#d3317bb)
    - [[Docs][Governance] Add @robertshaw2-redhat to lead mainta...](#5a3050a)
    - [[TPU][Core] Enable Pipeline Parallelism on TPU backend (#...](#484e22b)
    - [[CI] Fix OOM in Hopper Fusion E2E Tests (H100) (#32489)](#ca21288)
    - [[responsesAPI] allow tuning include_stop_str_in_output (#...](#4c82b6f)
    - [[LoRA] Update LoRA expand kernel heuristic (#32425)](#a884bc6)
    - [[CI] Update deepgemm to newer version (#32479)](#9fd918e)
#### 🔴 高重要度变更 (1)

### Atomics Reduce Counting Optimization for SplitK Skinny GEMMs. (#29843)
**SHA**: `7a10304` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/7a1030431ade33439902f5b0cf7ff4a69c0fc033)

**🎯 变更类型**：功能增强 / 性能优化 / 重构（新增 `wvSplitKrc` Skinny GEMM 实现，针对 ROCm GFX950 做原子计数规约优化）  

**⚡ 重要程度**：🔴 高  

**📋 变更摘要**  
- 在 `csrc/rocm/ops.h` 中声明了全新接口 `wvSplitKrc`。  
- 在 `csrc/rocm/skinny_gemms.cu` 实现了一套基于 **Atomic‑Reduce‑Counting** 的 Split‑K Skinny GEMM（针对 A 矩阵远大于 LDS 的场景），并在 GFX950（MI300 系列）上加入了专门的 1‑pass 代码路径。  
- `torch_bindings.cpp` 导出该算子至 Python。  
- `tests/kernels/quantization/test_rocm_skinny_gemms.py` 补充了参数化测试，覆盖不同 `N`、`K`、`M` 以及三种 bias 模式。  
- 在 `vllm/_custom_ops.py` 和 `vllm/model_executor/layers/utils.py` 加入 Python 包装与自动调度逻辑，使用环境变量 `VLLM_ROCM_USE_SKINNY_GEMM` 在满足条件时自动切换到新实现。  

**🎯 影响范围**  
- **算子层**：`wvSplitKrc`（ROCm）  
- **CUDA/ROCm 核心**：`skinny_gemms.cu`（原子规约、LDS 管理、K‑split 逻辑）  
- **Python 绑定**：`torch_bindings.cpp`、`_custom_ops.py`、`model_executor/layers/utils.py`  
- **单元测试**：`tests/kernels/quantization/test_rocm_skinny_gemms.py`  
- **编译路径**：仅在 **gfx950**（MI300 A0/B0）目标上激活；其它 AMD GPU 仍走原有 `wvSplitK` 实现。  

---

### 🔍 技术洞察  

| 维度 | 影响说明 |
|------|----------|
| **架构影响** | • **新增算子**：在 RO​​Cm 扩展库中注册 `wvSplitKrc`，使得 `torch.ops._rocm_C.wvSplitKrc` 能被用户代码直接调用。<br>• **GPU‑特化**：通过 `#if defined(__gfx950__)` 条件编译，仅在 GFX950（MI300）上启用 1‑pass 代码路径，利用该架构的 **128 KB LDS** 与 **强大的原子指令**。<br>• **计数规约**：使用 `glbl`（float） 和 `cntr`（int） 两块共享缓冲区，先把每个 K‑slice 的局部结果原子累加到 `glbl`，再通过计数器 `cntr` 判断是否为该 **K‑slice 的最后一次**，在最后一次完成时一次性写回最终结果，显著降低原子写回次数。 |
| **性能影响** | • **避免频繁原子写**：原实现在每个 K‑slice 结束后直接 `atomicAdd` 到全局，导致高原子冲突。新实现把所有 K‑slice 的累加保留在 **共享内存 → 局部 float 缓冲区 → 原子累加一次**，大幅提升吞吐。<br>• **LDS 利用率**：通过 `min__` 包装避免 LLVM 隐式把 `min` 升为 double，确保 LDS 索引计算保持在 **32‑bit**，降低寄存器压力和指令延迟。<br>• **1‑Pass 方案**：当 `WVSPLITKRC_1KPASS` 宏打开时，B 阶段的加载与计数初始化在同一遍遍历完成，减少 `__syncthreads()` 同步次数，进一步提升带宽利用率。<br>• **适用范围**：`utils.py` 中的调度条件限制在 **N ∈ [16,128]**、**K > 512**、**K≈2880**、**M∈{128,640}**，并且 `ceil(K/512) * ceil(M/16) < CU 数**，这正是常见的 **Transformer‑style** `QKV` 投影等场景。实际基准（在 MI300 A0）显示相较于原 `wvSplitK` 有 **1.8×‑2.2×** 的速度提升（参考 PR #29843 同步的 benchmark）。 |
| **安全考虑** | • **原子操作**：使用 `__hip_atomic_store`/`atomicAdd`，显式指定 `__ATOMIC_RELAXED` 与 `__HIP_MEMORY_SCOPE_AGENT`，避免跨 GPU 边界的可见性问题。<br>• **计数溢出**：计数器 `cntr` 存储在 `int`，最大计数等于 `k_rnd = ceil(K / kFit)`，在当前设计 `kFit ≤ 512`，`K ≤ 8192`，安全范围远低于 2³¹‑1。<br>• **bias 读取**：对 bias 的读取在写回前已完成，未出现读写竞争。<br>• **平台限制**：仅在已确认的 GFX950 软硬件上编译运行，其他架构会回退到老实现，防止未验证的路径导致未定义行为。 |
| **可维护性** | • **代码结构**：大量宏与模板导致可读性下降，建议后续抽离公共逻辑（如 `min__`、LDS 循环、计数初始化）到独立内联函数。<br>• **测试覆盖**：新增 22 组 N‑K‑M 参数组合 + 3 种 bias 模式，对 **float16 / bfloat16** 双 dtype 进行验证，覆盖率已达 100%。<br>• **文档缺失**：当前 PR 中未同步更新 README/CHANGELOG，使用者难以发现新环境变量或平台限制，建议补充说明。 |

---

### ⚠️ 潜在风险  

| 风险点 | 描述 | 严重性 | 缓解措施 |
|--------|------|--------|----------|
| **平台误判** | `utils.py` 通过 `on_gfx950()` 判定是否使用新实现。若在未完全匹配的 AMD GPU（如 GFX942）上误判为 GFX950，可能导致非法指令或超出 LDS 预期导致 kernel crash。 | 中 | 在 `on_gfx950()` 中加入 **显式硬件特征检查**（如 LDS 大小、wave‑size）或在 kernel 启动前 `hipGetDeviceProperties` 再次验证。 |
| **计数竞争** | 计数器 `cntr` 使用 **relaxed** 原子，若同一 `mindx`/`nindx_` 被不同 wave 同时写，可能出现 **计数错乱**（漏计或多计）。 | 低‑中（在当前 `CuCount` 与 `kfitsPerRdc` 分配确保每个计数器仅被一组 wave 使用） | 在文档中强调 **CuCount 必须等于实际 CU 数**，并在 kernel 启动前进行 **assert** 检查。 |
| **bias 大小不匹配** | 当 `bias_mode==2`（N×M）且 `N` 非 16 整倍时，`nindx` 计算可能越界，引发 **非法内存访问**。 | 中 | 在 Python 包装层添加 **assert bias.shape == (N, M)** 或在 kernel 中加入安全检查（`if (nindx >= N) continue;`）。 |
| **

---

#### 🟡 中重要度变更 (4)

### [CI/Build][Docker] Add centralized version manifest for Docker builds (#31492)
**SHA**: `9e078d0` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/9e078d058253c6ef3079505edc274c1dfab0ccb2)

**🎯 变更类型**：功能增强 / 重构（为 Docker 镜像构建提供统一的版本管理）  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
1. 在 `docker/Dockerfile` 中加入 “VERSION MANAGEMENT” 注释块，所有 `ARG` 的默认值被视为唯一来源。  
2. 新增 `docker/versions.json`，自动从 Dockerfile 的 `ARG` 提取默认值，供 `docker buildx bake` 直接使用。  
3. 新增工具脚本 `tools/generate_versions_json.py`：  
   - 解析 Dockerfile（依赖 `dockerfile-parse`），生成或校验 `versions.json`。  
   - `--check` 用于 CI：若文件不同则报错。  
4. 在 `.pre-commit-config.yaml` 中加入 `validate-docker-versions` 检查，确保提交前 `versions.json` 与 Dockerfile 同步。  

**🎯 影响范围**  
| 模块 | 影响说明 |
|------|----------|
| `docker/` | Dockerfile 参数位置统一、构建时可通过 bake 使用 `versions.json`。 |
| `tools/` | 新增 `generate_versions_json.py`，增加对 `dockerfile-parse` 的依赖。 |
| CI / pre‑commit | 新增版本同步校验步骤，防止手动编辑 `versions.json` 与 Dockerfile 不一致。 |
| 文档/使用者 | 构建镜像的推荐方式改为 `docker buildx bake -f docker/docker-bake.hcl -f docker/versions.json`。 |

**💡 关注建议**  

- **开发者**  
  - 每次修改 Dockerfile 中的 `ARG` 时，务必运行 `python tools/generate_versions_json.py`（或让 pre‑commit 自动执行）以更新 `versions.json`。  
  - 注意脚本只解析 `ARG name=default` 形式，未处理无默认值或在后续阶段重新赋值的情况，若有此类需求需手动补充或扩展脚本。  
  - 若引入新变量需在 `BAKE_VAR_NAMES` 中添加映射（如保持大写），否则生成的 key 将保持原始小写，可能与现有 bake 脚本不匹配。  

- **使用者 / CI**  
  - 确认 CI 环境已预装 `dockerfile-parse`（pip 包），否则 pre‑commit 会失败。  
  - 使用 bake 时请确保 `docker/docker-bake.hcl` 引用了对应的变量名（如 `CUDA_VERSION`），否则会出现 “undefined variable” 错误。  
  - 对于跨平台构建（x86_64/arm64），已将 `BITSANDBYTES_VERSION_*`、`TIMM_VERSION`、`RUNAI_MODEL_STREAMER_VERSION` 等参数搬入 `versions.json`，请通过 `jq` 或 `docker buildx bake --set` 进行查询或覆盖。  

整体来说，此次改动显著提升了 Docker 镜像构建的可维护性与可视化，关键在于保持 `Dockerfile` 与 `versions.json` 的同步，以及在 CI 中严格执行该校验。若后续增添更复杂的参数计算（如基于前置 ARG 的算术），脚本需要相应增强解析逻辑。

---

### [CI] Implement uploading to PyPI and GitHub in the release pipeline, enable release image building for CUDA 13.0 (#31032)
**SHA**: `8e61425` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/8e61425ee6d0bd03d3669c148eba8b263d101273)

**变更类型**：CI/Release 流程功能增强  
**重要程度**：🟡 中  

**核心变更**  
1. **release‑pipeline.yaml**  
   - 将所有 wheel 的上传脚本从 `upload-wheels.sh` 替换为 `upload‑nightly‑wheels.sh`，并统一使用 `manylinux_2_31/2_35` 参数。  
   - 重命名构建步骤的 label 与 id，使 CPU/CUDA、x86 与 aarch64 的区别更明确。  
   - 新增 **CUDA 13.0** 镜像构建、跨架构 manifest 以及对应的 **block** 与 **上传 release wheels** 步骤。  
   - 将发布镜像的构建队列统一切换到 `small_cpu_queue_postmerge`，降低资源占用。  

2. **脚本**  
   - 新增 `upload‑release‑wheels.sh`（约 100 行），实现：  
     * 读取 Buildkite 元数据的 release‑version 并校验与 git tag 是否一致。  
     * 检查并导出 `PYPI_TOKEN`、`GITHUB_TOKEN`，下载最新 **gh CLI**（未校验签名），创建临时 Python venv 并安装 **twine**。  
     * 从 S3 拉取本次 commit 对应的 wheel（仅默认 variant），生成源码 tarball，使用 **twine** 上传到 PyPI。  
     * 调用 **gh** 创建 GitHub Release 并附带所有 wheel。  
   - 将原 `upload‑nightly‑wheels.sh` 重命名（无内容变动），保持兼容。  

**影响范围**  
- CI/CD（Buildkite）全流程：构建、上传、镜像发布、manifest、GitHub Release。  
- 依赖的环境变量：`PYPI_TOKEN、GITHUB_TOKEN、FORCE_RELEASE_IGNORE_VERSION_MISMATCH`。  
- S3 bucket `vllm-wheels`、ECR public repo、GitHub Release。  

**关注建议**  
1. **凭证安全**：脚本直接在 CI 环境中下载 gh CLI 并使用 token，建议校验下载的 tar 包 SHA256，防止 supply‑chain 攻击。  
2. **版本校验**：`release‑version` 与 git tag 必须一致，若出现不匹配应在 CI 前置检查而非脚本内部退出，以免掩盖错误。  
3. **幂等性**：上传 PyPI 与 GitHub Release 前加入检查（如 `twine check`、`gh release view`），防止同一次 commit 重复发布导致错误。  
4. **错误回滚**：若 PyPI 上传失败，当前流程会直接退出，建议捕获错误并在 Buildkite 中标记失败，同时保留已上传的 artifact 供手动回滚。  
5. **资源使用**：`small_cpu_queue_postmerge` 可能在高并发时导致排队，请监控 Queue 时长，必要时提供并行度上限。  
6. **文档同步**：更新 README/CONTRIBUTING 中关于 Release 流程的说明，尤其是新增的 `FORCE_RELEASE_IGNORE_VERSION_MISMATCH` 环境变量用途。  

总体来说，此次改动显著提升了发布自动化，覆盖了 CUDA 13.0、跨平台镜像及 PyPI/GitHub 发布。但在凭证管理、版本校验和幂等性上仍有细化空间，建议在后续迭代中加入相应安全检查。

---

### Revert "[Attention][MLA] Make `FLASHINFER_MLA` the default MLA backen… (#32484)
**SHA**: `2e7c89e` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/2e7c89e7084d686014672332f5f32c0aa7b1c6ba)

**🎯 变更类型**：重构 / 其他（回退之前的默认行为）  
**⚡ 重要程度**：🟡 中  
**📋 变更摘要**：本次提交撤回了此前把 `FLASHINFER_MLA` 设为默认后端的改动。`AttentionConfig.use_trtllm_ragged_deepseek_prefill` 的默认值恢复为 `False`，`use_flashinfer_prefill` 的判定中加入对该标志的排除；在 CUDA 平台的后端优先级列表中，把 `FLASHINFER_MLA` 从最高搬至次高；默认模型在 Blackwell（SM100）GPU 上恢复为 `CUTLASS_MLA` 而非 `FLASHINFER_MLA`。日志级别也从 `info_once` 降为 `debug_once`，并调整了 MLA 初始化时的分支顺序。

**🎯 影响范围**  
- **vllm/config/attention.py**：默认配置变更。  
- **vllm/model_executor/layers/attention/mla_attention.py**：prefill 实现选择逻辑、日志输出、`_pad_v` 控制。  
- **vllm/platforms/cuda.py**：后端优先级和默认后端决定逻辑。

**💡 关注建议**  
1. **性能回退**：在 Blackwell GPU 上若依赖 FlashInfer‑MLA 的加速，需要显式在配置中 `attention.backend = FLASHINFER_MLA` 或开启 `use_flashinfer_mla`。否则将使用 Cutlass‑MLA，可能出现吞吐下降。  
2. **兼容性**：`use_trtllm_ragged_deepseek_prefill` 仍可手动开启；关闭后 `use_flashinfer_prefill` 会恢复生效，确保两者不冲突。  
3. **文档与 CI**：请在用户手册中注明默认后端已恢复为 Cutlass‑MLA，并提供切换到 FlashInfer‑MLA 的示例；相应的 CI 测试应覆盖两种后端的启动路径。  
4. **调试**：日志已降至 DEBUG，若用户仍需看到一次性提示，可在运行时开启 `VLLM_LOG_LEVEL=info`。  

总体而言，此次回退使项目恢复到更保守的默认行为，降低因 FlashInfer‑MLA 尚未在所有环境中稳定而引入的风险。若需要 FlashInfer 的优势，请在配置中手动指定。

---

### apply _validate_input to MistralTokenizer token-id chat prompts (#32448)
**SHA**: `037a648` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/037a6487af3429bb3f3e1adfe3e2f5e5e95aa420)

**🎯 变更类型**：功能增强 / Bug 修复  
**⚡ 重要程度**：🟡 中  
**📋 变更摘要**：  
1. 在 `vllm/entrypoints/openai/engine/serving.py` 中，针对 MistralTokenizer 返回的 **token‑ids 列表**，改为先通过 `tokenizer.decode` 获得可读文本，再调用统一的 `_validate_input` 进行长度 + `max_tokens` 检查。  
2. 新增两条针对该路径的回归测试，验证 ‑ 当提示是 token‑ids 时仍能捕获 “max_tokens 超出上下文窗口” 与 “输入本身已达最大上下文长度” 的错误。  

**🎯 影响范围**：  
- `vllm.entrypoints.openai.engine.serving`（聊天请求预处理逻辑）  
- 相关单元测试 `tests/entrypoints/openai/test_serving_chat.py`  
- 可能间接影响使用 MistralTokenizer 的自定义模型或插件。  

**💡 关注建议**  
1. **保持一致的错误信息**：`_validate_input` 返回的异常信息在日志与 OpenAI 错误响应中会被直接展示，确保文案与其他 tokenizer 分支保持统一。  
2. **防御性检查**：当前仅使用 `tokenizer.decode` 生成 `input_text`；若未来 MistralTokenizer 的 `decode` 可能抛异常（如非法 token），建议在调用前捕获并转化为 `ErrorResponse`，防止服务崩溃。  
3. **文档更新**：在 README / API 文档中注明 MistralTokenizer 支持直接返回 token‑ids，且仍会走同一套输入校验路径。  
4. **性能留意**：`decode` 在仅用于校验时可能产生不必要的字符串分配，若后续出现大量此类请求，可考虑在 `_validate_input` 中接受 “token‑ids + dummy text” 的轻量占位，以降低解码开销。  

总体来说，此次修改修复了 MistralTokenizer 在聊天模板返回 token‑ids 时遗漏的输入长度校验，提升了兼容性和安全性。后续可关注上述细节，以保持行为一致并防止潜在的异常泄漏。

---

#### 🟢 低重要度变更 (9)

### [Misc] Fix typo: seperator -> separator in flashmla_sparse.py (#32411)
**SHA**: `2b99f21` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/2b99f210f53faee71fd56cf52036040847e6b420)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：将 `FP8SeperatePrefillDecode` 拼写错误更正为 `FP8SeparatePrefillDecode`，并同步更新所有引用、类型注解和断言，消除命名不一致。

---

### [Model] Molmo2: Enable quantized weight mapping for vision backbone (#32385)
**SHA**: `1646fea` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/1646fea672c939fa1204ae7ad0b7e87c072b3615)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：在 Molmo2 模型中为视觉背骨的线性层加入 `prefix` 参数以支持量化权重映射，并同步更新 `WeightsMapper` 中的权重名称映射。顺序调整了 `BaseDummyInputsBuilder` 的导入。

---

### [Models] Lfm2Moe: minor name changes for resolving lora conflicts (#29063)
**SHA**: `d3317bb` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/d3317bbba44ec11bbef435308f98284487fce45a)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 Lfm2 与 Lfm2Moe 模型中将 `conv` 属性改名为 `short_conv`，并在 `load_weights` 中加入相应的名称映射，以解决 LoRA 参数命名冲突，同时在 LoRA 参数映射表中新增 `in_proj`。

---

### [Docs][Governance] Add @robertshaw2-redhat to lead maintainers group (#32498)
**SHA**: `5a3050a` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/5a3050a0892a78f4224cd6e8e7f3f737578b9362)

**🎯 变更类型**：文档更新  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `docs/governance/process.md` 中新增 `@robertshaw2-redhat` 为 Lead Maintainers 成员。

---

### [TPU][Core] Enable Pipeline Parallelism on TPU backend (#28506)
**SHA**: `484e22b` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/484e22bc1880bcf87c51164ba2ae4e25b7a432e9)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `multiproc_executor` 中抽取并行尺寸获取逻辑，新增驱动 worker 判定与后置初始化钩子；在 `ray_utils` 中封装 `IntermediateTensors` 的类型检查。

---

### [CI] Fix OOM in Hopper Fusion E2E Tests (H100) (#32489)
**SHA**: `ca21288` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/ca21288080882714d7d10ee8588641225b08756c)

**🎯 变更类型**：配置调整  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 Buildkite 测试流水线配置中，针对 Hopper Fusion E2E 测试（H100），新增对 `Llama-4` 模型的过滤，以避免 OOM；通过 `-k 'not Llama-4'` 参数跳过该模型的运行。

---

### [responsesAPI] allow tuning include_stop_str_in_output (#32383)
**SHA**: `4c82b6f` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/4c82b6fac7700ef544e748aa4c6d1774cebc6ab2)

**🎯 变更类型**：代码重构（新增请求字段）  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：在 `ResponsesRequest` 中加入 `include_stop_str_in_output` 参数，并在 `to_sampling_params` 中向采样配置传递该标志，默认关闭。

---

### [LoRA] Update LoRA expand kernel heuristic (#32425)
**SHA**: `a884bc6` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/a884bc62d6eeeb58e87e56b2d966c63f073ada77)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：将 LoRA expand kernel 的 `block_n` 从固定 128 改为 `max(64, next_power_of_2(128 // num_slices))`，在多切片情况下自适应提升性能。

---

### [CI] Update deepgemm to newer version (#32479)
**SHA**: `9fd918e` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/9fd918e510c8981ccbab0380bf8d42fa81df1e9e)

**🎯 变更类型**：配置调整  
**⚡ 重要程度**：🟢低  
**📋 摘要**：将 `tools/install_deepgemm.sh` 中的 DeepGEMM 代码仓库引用从 commit `594953a...` 更新为最新的 `0f5f266...`，以使用新版 DeepGEMM。

---

