# 每日更新报告（2026-01-31）

## vllm-project/vllm

| 提交时间 | 作者 | 提交信息 |
|----------|------|----------|
| 2026-01-31 23:22:25 | jma99_2333 | Support clear mm and encoder cache (#33452) |
| 2026-01-31 23:12:17 | ℍ𝕠𝕝𝕝𝕠𝕨 𝕄𝕒𝕟 | [BugFix][Router Replay] Capture Logical Experts with EPLB (#33013) |
| 2026-01-31 22:48:34 | Luka Govedič | [fix][torch.compile] Fix cold-start compilation time increase by adding kv cache update to splitting ops (#33441) |
| 2026-01-31 22:48:28 | Cyrus Leung | [Doc] Update plugin deprecation notices (#33476) |
| 2026-01-31 22:04:20 | cmunley1 | support return prompt token ids in responses  (#33378) |
| 2026-01-31 21:38:39 | Roy Wang | [Misc] Replace deprecated interface seed_everything (#33474) |
| 2026-01-31 21:21:32 | Cyrus Leung | [Bugfix] Fix incompatibility between #33372 and #32863 (#33475) |
| 2026-01-31 21:00:54 | Angela Yi | [ez] Add structured torch.compile logs (#33213) |
| 2026-01-31 20:51:15 | Cyrus Leung | [Frontend] Use new Renderer for Completions and Tokenize API (#32863) |
| 2026-01-31 17:51:26 | caozuoba | [perf] v1/spec_decode: skip softmax for all-greedy rejection sampling (#32852) |
| 2026-01-31 17:03:57 | jennyyyyzhen | [ROCM] Enable aiter attn backend for qwen3-next model (#32492) |
| 2026-01-31 16:14:54 | Jinwu | [BugFix] Add synchronize in CutlassW4A8LinearKernel to ensure data is ready for use. (#33078) |
| 2026-01-31 15:38:46 | Yanan Cao | [Kernel] [Helion] [3/N] Helion kernel registry (#33203) |
| 2026-01-31 15:16:22 | Fadi Arafeh | [CPU][Feat] Enable KleidiAI accelerated int4 dynamic quant with BF16 activations on Arm CPUs (#33122) |
| 2026-01-31 14:53:08 | AutumnAurelium | Add EAGLE3 support for AFMoE (#33111) |
| 2026-01-31 14:48:27 | Dimitrios Bariamis | Add support for Mistral Large 3 inference with Flashinfer MoE (#33174) |
| 2026-01-31 14:21:51 | Matthias Gehre | [Bugfix] Handle Asym W4A16 (ConchLinearKernel) for CT (#33200) |
| 2026-01-31 13:24:49 | Nicolò Lucchesi | [Bugfix] Fix `Qwen3ASR` language asr tag in output  (#33410) |
| 2026-01-31 12:53:01 | Yanan Cao | [Kernel] [Helion] [2/N] Helion kernel wrapper (#32964) |
| 2026-01-31 12:50:30 | Francesco Fusco | [Attention] Clarify comment explaining attn_logits +1 dimension (#33427) |
| 2026-01-31 12:49:00 | Patrick von Platen | [Streaming -> Realtime] Rename all voxtral related classes, fn, files (#33415) |
| 2026-01-31 12:28:29 | Micah Williamson | [ROCm][CI] Force max_num_seqs=1 on ROCm In test_sharded_state_loader to reduce flakiness (#33277) |
| 2026-01-31 12:19:22 | Russell Bryant | [Misc] offest -> offset in comments and variable names (#33444) |
| 2026-01-31 12:15:06 | Lucas Wilkinson | [BugFix] Fix whisper FA2 + full cudagraphs (#33360) |
| 2026-01-31 12:14:54 | Michael Goin | [UX] Use gguf `repo_id:quant_type` syntax for examples and docs (#33371) |
| 2026-01-31 11:36:56 | Isotr0py | [Misc] Algin Qwen3-VL-embedding image example outputs with HF repo example (#33419) |
| 2026-01-31 11:33:26 | Nick Hill | [ModelRunner V2] Fix spec decoding + logprobs (#33391) |
| 2026-01-31 11:30:00 | Matthew Bonanni | [Attention] Move MLA `forward` from backend to layer (#33284) |
| 2026-01-31 10:54:16 | Wentao Ye | [Deprecation] Deprecate `seed_everything` and `scatter_mm_placeholders` in v0.15 (#33362) |
| 2026-01-31 09:26:15 | Alberto Ferrer | [Bugfix] Fix typo in read_offset variable name (#33426) |
| 2026-01-31 09:05:23 | Gregory Shtrasberg | [Bugfix][ROCm] Fixing the skinny gemm dispatch logic from #32831 (#33366) |
| 2026-01-31 08:37:42 | Michael Goin | Refactor NVFP4 Linear utils for ModelOpt and CT (#33201) |
| 2026-01-31 05:36:29 | Chendi.Xue | [CI][HPU]accelerate hpu test by skip python re-install and clean container name (#33286) |
| 2026-01-31 04:34:36 | Huy Do | Indicate compile mode in the benchmark results (#32990) |
| 2026-01-31 02:30:46 | Pavani Majety | [Hardware][SM100] Add TRTLLM Kernel for INT4 W4A16 Kernel. (#32437) |
| 2026-01-31 01:50:23 | xuebwang-amd | [Quantization][ROCm] Fix MoE weight loading to be robust (Qwen3_MoE/Qwen3_next as example models) (#33173) |
| 2026-01-31 01:29:09 | Vasiliy Kuznetsov | fix QERL attention import path (#33432) |
| 2026-01-31 01:19:19 | Yanan Cao | [Kernel] [Helion] [1/N] Add Helion ConfigManager (#32740) |
| 2026-01-31 00:30:10 | Harry Mellor | Fix encoder-decoder model disabling mm processor cache (#33236) |
| 2026-01-31 00:17:56 | Nicolò Lucchesi | [CI] Qwen3-ASR transcriptios tests (#33414) |
| 2026-01-31 00:15:20 | Michael Goin | Support FP8 block quant for CompressedTensorsW8A16Fp8 (#33280) |

### 📊 统计摘要
> 本日共 41 个提交 | 🔴高 4 | 🟡中 16 | 🟢低 21
## 📋 目录

- [vllm-project/vllm](#vllm-project-vllm)
  - [📊 统计摘要](#-统计摘要)
  - [🔴 高重要度变更 (4)](#-🔴-高重要度变更-4)
    - [[Frontend] Use new Renderer for Completions and Tokenize ...](#f0a1c84)
    - [[Attention] Move MLA `forward` from backend to layer (#33...](#aaa901a)
    - [Refactor NVFP4 Linear utils for ModelOpt and CT (#33201)](#67ebaff)
    - [[Hardware][SM100] Add TRTLLM Kernel for INT4 W4A16 Kernel...](#c3a9752)
  - [🟡 中重要度变更 (16)](#-🟡-中重要度变更-16)
    - [Support clear mm and encoder cache (#33452)](#22d9a05)
    - [[BugFix][Router Replay] Capture Logical Experts with EPLB...](#13b842f)
    - [[fix][torch.compile] Fix cold-start compilation time incr...](#15f40b2)
    - [[Bugfix] Fix incompatibility between #33372 and #32863 (#...](#4cb59de)
    - [[ez] Add structured torch.compile logs (#33213)](#608b556)
    - [[Kernel] [Helion] [3/N] Helion kernel registry (#33203)](#d5c41db)
    - [Add support for Mistral Large 3 inference with Flashinfer...](#f0bca83)
    - [[Bugfix] Fix `Qwen3ASR` language asr tag in output  (#33410)](#e77f162)
    - [[Kernel] [Helion] [2/N] Helion kernel wrapper (#32964)](#8ecd213)
    - [[Streaming -> Realtime] Rename all voxtral related classe...](#15e0bb9)
    - [[UX] Use gguf `repo_id:quant_type` syntax for examples an...](#29fba76)
    - [[ModelRunner V2] Fix spec decoding + logprobs (#33391)](#876a16f)
    - [[Deprecation] Deprecate `seed_everything` and `scatter_mm...](#010ec0c)
    - [[Bugfix][ROCm] Fixing the skinny gemm dispatch logic from...](#31aedfe)
    - [[Kernel] [Helion] [1/N] Add Helion ConfigManager (#32740)](#6c1f9e4)
    - [Support FP8 block quant for CompressedTensorsW8A16Fp8 (#3...](#fd0e377)
  - [🟢 低重要度变更 (21)](#-🟢-低重要度变更-21)
    - [[Doc] Update plugin deprecation notices (#33476)](#793af53)
    - [support return prompt token ids in responses  (#33378)](#6f5e7cd)
    - [[Misc] Replace deprecated interface seed_everything (#33474)](#68feb76)
    - [[perf] v1/spec_decode: skip softmax for all-greedy reject...](#8980001)
    - [[ROCM] Enable aiter attn backend for qwen3-next model (#3...](#527bcd1)
    - [[BugFix] Add synchronize in CutlassW4A8LinearKernel to en...](#f68e3ea)
    - [[CPU][Feat] Enable KleidiAI accelerated int4 dynamic quan...](#1618e25)
    - [Add EAGLE3 support for AFMoE (#33111)](#f3888ac)
    - [[Bugfix] Handle Asym W4A16 (ConchLinearKernel) for CT (#3...](#73419ab)
    - [[Attention] Clarify comment explaining attn_logits +1 dim...](#5b55c0b)
    - [[ROCm][CI] Force max_num_seqs=1 on ROCm In test_sharded_s...](#6c64c41)
    - [[Misc] offest -> offset in comments and variable names (#...](#a2ef06e)
    - [[BugFix] Fix whisper FA2 + full cudagraphs (#33360)](#0a3c71e)
    - [[Misc] Algin Qwen3-VL-embedding image example outputs wit...](#9df152b)
    - [[Bugfix] Fix typo in read_offset variable name (#33426)](#64a40a7)
    - [[CI][HPU]accelerate hpu test by skip python re-install an...](#2b46557)
    - [Indicate compile mode in the benchmark results (#32990)](#9ca66ec)
    - [[Quantization][ROCm] Fix MoE weight loading to be robust ...](#f451b45)
    - [fix QERL attention import path (#33432)](#3f96fcf)
    - [Fix encoder-decoder model disabling mm processor cache (#...](#67239c4)
    - [[CI] Qwen3-ASR transcriptios tests (#33414)](#8ece607)
#### 🔴 高重要度变更 (4)

### [Frontend] Use new Renderer for Completions and Tokenize API (#32863)
**SHA**: `f0a1c84` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/f0a1c8453ad1c664c8a04c83fe545195fcd556eb)

⚠️ LLM分析失败（已重试3次）: API请求失败: HTTPSConnectionPool(host='integrate.api.nvidia.com', port=443): Read timed out. (read timeout=30)

*暂无分析*

---

### [Attention] Move MLA `forward` from backend to layer (#33284)
**SHA**: `aaa901a` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/aaa901ad55ee454f061649b4cc24f96c5ea04e33)

⚠️ LLM分析失败（已重试3次）: API请求失败: HTTPSConnectionPool(host='integrate.api.nvidia.com', port=443): Read timed out. (read timeout=30)

*暂无分析*

---

### Refactor NVFP4 Linear utils for ModelOpt and CT (#33201)
**SHA**: `67ebaff` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/67ebaff528be7faef14fa4eb86cfd8c4b5714e38)

**🎯 变更类型**：功能增强 / 重构 / 架构变更  
**⚡ 重要程度**：🔴 高  
**📋 变更摘要**：  
- 将 NVFP4（NVIDIA FP4）线性算子相关的工具函数、后端选择与权重预处理统一抽取到 `vllm/model_executor/layers/quantization/utils/nvfp4_utils.py`。  
- 移除原有 `quant_utils` 中的重复实现（`swizzle_blockscale`、`cutlass_fp4_supported`、权重/激活 padding 与切片 等），并更新所有调用点（Compressed‑Tensors、ModelOpt、MoE、Marlin）使用新的统一 API。  
- 简化 `process_weights_after_loading` 与 `apply` 的实现，统一使用 `select_nvfp4_linear_backend()` 与 `apply_nvfp4_linear()`，并统一命名（`input_global_scale`、`weight_global_scale`）。  
- 相应测试文件和 import 路径随之迁移。  

---

## 🎯 影响范围
| 受影响模块 / 组件 | 关键改动点 |
|-------------------|------------|
| `vllm.model_executor.layers.quantization.utils.nvfp4_utils` (新文件) | 新增后端枚举、统一选型、权重/激活准备、线性前向实现、`swizzle_blockscale`、`cutlass_fp4_supported`、padding/切片等公共工具 |
| `compressed_tensors` 相关方案 (`compressed_tensors_w4a4_nvfp4.py`、`compressed_tensors_w4a16_nvfp4.py`) | 删除对 `envs.VLLM_USE_NVFP4_CT_EMULATIONS` 的条件分支，改用统一 `apply_nvfp4_linear`，简化 `process_weights_after_loading` |
| `modelopt` 量化实现 (`modelopt.py`) | 替换原有 `envs`/`cutlass` 逻辑为 `select_nvfp4_linear_backend()`，统一 `process_weights_after_loading` 与 `apply` |
| `marlin_utils_fp4.py`、`nvfp4_moe_support.py`、`flashinfer_fp4_moe.py` | 参数名从 `weight_scale_2` → `weight_global_scale`，调用新的 `apply_nvfp4_linear` / `prepare_fp4_layer_for_marlin` |
| 测试文件 (`tests/kernels/moe/...`、`tests/quantization/...`) | 更新 import 为 `nvfp4_utils`，确保仍能跑通 |
| 其他文件 (`quant_utils.py`) | 删除已迁移的函数实现，保持精简 |

---

## 🔍 技术洞察

### 1. 架构影响
| 维度 | 影响 |
|------|------|
| **模块化 & 可维护性** | 将散落在多个文件中的 NVFP4 相关逻辑集中到 `nvfp4_utils.py`，形成单一职责的工具层。后续新增/调整后端只需在此文件修改，降低跨文件耦合。 |
| **统一后端选择** | 通过 `select_nvfp4_linear_backend()` 自动检测环境变量、平台能力、FlashInfer/Marlin/FBGEMM 可用性，统一返回 `NvFp4LinearBackend` 枚举。消除了之前在每个量化实现里重复的 “if‑else” 逻辑。 |
| **统一前向接口** | `apply_nvfp4_linear()` 负责所有后端的前向计算（Cutlass、FlashInfer‑Cutlass、FlashInfer‑TRTLLM、FBGEMM、Marlin、Emulation），调用方只需传 `backend`、`layer`、`x`、`bias`。这简化了 `CompressedTensors*`、`ModelOpt*` 等类的实现。 |
| **命名统一** | 采用 `input_global_scale` / `weight_global_scale` 替代旧的 `input_scale` / `weight_scale_2`，提升含义明确性，兼容 ModelOpt 与 CT（Compressed‑Tensors）两个生态。 |
| **后端抽象层** | 对每个后端的特殊准备（如 FlashInfer‑TRTLLM 的 shuffle、Cutlass 的 padding & swizzle、FBGEMM 的 uint8 block‑scale）均在 `convert_to_nvfp4_linear_kernel_format()` 中实现，保持上层代码干净。 |

### 2. 性能影响
| 维度 | 正面影响 | 潜在影响 |
|------|----------|----------|
| **后端自动选择** | 默认优先使用 *FlashInfer‑Cutlass*（若有）或 *Cutlass*，后者在 SM≥8.0 上已经高度优化，能获得 1.5‑2× 的吞吐提升相较于原先的显式 `cutlass_fp4_supported()` 分支。 | 若用户显式依赖旧的 `VLLM_NVFP4_GEMM_BACKEND`（如强制 `marlin`），仍能保持原有行为，但未在新路径中加入 `marlin` 的 *K‑dim padding* 逻辑（Marlin 自己处理），需确保外部已做好准备。 |
| **权重/激活预处理统一** | `prepare_weights_for_nvfp4_cutlass` 只在一次转换里完成 padding + swizzle，避免在每次前向重新进行。| 对 FlashInfer‑TRTLLM 后端仍保留一次 shuffle，每次 forward 用 `apply_fp4_marlin_linear`（Marlin）不再需要 `alpha` 参数计算，略微提升 runtime。 |
| **全局 Scale 处理** | 将 `input_global_scale_inv` 与 `weight_global_scale` 在 `process_weights_after_loading` 中一次性转为标量（`max()`），在 forward 中直接使用，减少 `max` 调用次数。| 对极端稀疏/不均匀 Scale（不同张量间 Scale 差异大）的 checkpoint，可能出现数值误差放大，需要验证数值一致性。 |
| **代码路径简化** | `apply_nvfp4_linear` 统一返回 `out.view(*output_shape)`，削减了多余的 reshape / view 抖动。| 仍保留 `slice_nvfp4_output`，如果 Padding 计算不准确会导致输出裁剪错误。 |

### 3. 安全考虑
| 项目 | 说明 |
|------|------|
| **环境变量** | 新增对 `VLLM_USE_FBGEMM`、`VLLM_USE_NVFP4_CT_EMULATIONS`、`VLLM_NVFP4_GEMM_BACKEND` 的读取。若用户自行设置错误的值（例如 `VLLM_USE_FBGEMM=1` 但未安装 `fbgemm-gpu-genai`），将抛出明确的 `ImportError`，无隐蔽崩溃。 |
| **异常检查** | `select_nvfp4_linear_backend()` 对每种后端执行 `assert` 检查；若平台不支持相应功能（如缺少 FlashInfer，或 GPU 能力不足），会在初始化阶段报错，避免在推理期间产生未定义行为。 |
| **类型安全** | 新增对 `weight_scale.dtype` 的检查（支持 `torch.float8_e4m3fn` 与 `torch.uint8`），确保后端接收正确的量化类型。 |
| **资源泄漏** | `process_weights_after_loading` 现在会 `del` 旧属性或用 `Parameter(..., requires_grad=False)` 替换，避免在模型加载后保留额外未使用的张量。 |

### 4. 兼容性 & 回归风险
| 风险点 | 说明 | 缓解建议 |
|--------|------|----------|
| **Checkpoint 参数名变化** | 老的 CT checkpoint仍保存 `input_scale`、`weight_scale_2` 等字段；在加载后通过 `process_weights_after_loading` 会重新创建 `input_global_scale`、`weight_global_scale`。若模型序列化方式在未来改动，可能出现缺失。 | 保持 `process_weights_after_loading` 中 `hasattr` 检查，确保在缺失旧字段时不抛异常；在文档中注明兼容旧 checkpoint。 |
| **后端切换行为差异** | 原实现在 `envs.VLLM_USE_NVFP4_CT_EMULATIONS` 为 `True` 时走 emulation。新实现仍支持，但内部 `select_nv

---

### [Hardware][SM100] Add TRTLLM Kernel for INT4 W4A16 Kernel. (#32437)
**SHA**: `c3a9752` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/c3a9752b0c11f87677e2ab918e524af7a368c664)

⚠️ LLM分析失败（已重试3次）: API请求失败: HTTPSConnectionPool(host='integrate.api.nvidia.com', port=443): Read timed out. (read timeout=30)

*暂无分析*

---

#### 🟡 中重要度变更 (16)

### Support clear mm and encoder cache (#33452)
**SHA**: `22d9a05` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/22d9a056d5c4849af372f9506bb93cad7da755f6)

**🎯 变更类型**：功能增强（新增 encoder cache 管理接口）  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
本次提交为 VLLM 服务端加入对 **encoder cache**（视觉编码器输出缓存）的统一清理能力。新增 `/reset_encoder_cache` HTTP 接口、相应的 RPC 与内部调度/执行层方法，并在 `EncoderCacheManager` 与 `EncoderDecoderCacheManager` 中实现 `reset()`，确保缓存状态、容量统计在模型权重更新或调试时能够被完整重置。同时在统计结构 `SchedulerStats` 中加入 `encoder_cache_usage`，并在文档中列出新 endpoint。

**🎯 影响范围**  
- **协议层**：`vllm/engine/protocol.py` 新增抽象方法 `reset_encoder_cache`。  
- **HTTP API**：`api_router.py` 暴露 `/reset_encoder_cache`。  
- **调度层**：`v1/core/sched/interface.py`、`scheduler.py` 实现并调用 `EncoderCacheManager.reset`。  
- **缓存实现**：`v1/core/encoder_cache_manager.py`（EncoderCacheManager、EncoderDecoderCacheManager）实现清理逻辑。  
- **执行层**：`v1/worker/gpu_model_runner.py`、`gpu_worker.py` 增加 GPU 端缓存清理。  
- **核心 Engine**：`core.py`、`llm_engine.py`、`async_llm.py` 等链路同步调用。  
- **统计 & 文档**：`stats.py` 增加 `encoder_cache_usage`；`security.md` 添加文档说明。  
- **测试**：`tests/v1/core/test_encoder_cache_manager.py` 覆盖了 reset 行为。

**💡 关注建议**  
1. **兼容性**：仅在开发模式下暴露 reset 接口，生产环境仍可通过环境变量控制，避免误触导致服务中断。  
2. **并发安全**：`reset_encoder_cache` 会在有运行请求时打印警告，建议在使用前确保请求已完成或在维护窗口执行。  
3. **监控**：新增的 `encoder_cache_usage` 需要在监控面板中展示，帮助运维评估缓存命中率。  
4. **测试覆盖**：CI 已加入单元测试，建议在多卡/分布式环境下做集成验证，确保各 worker 的 `reset_encoder_cache` 能被 RPC 正常触发。  
5. **文档更新**：README 与 API 手册应同步新增 endpoint 的使用示例及风险说明。  

总体来看，此功能为模型热更新、调试提供了必要的缓存清理手段，影响范围局部且实现方式统一，风险可控。建议在下一次部署时开启该 endpoint 并在监控中观察缓存使用情况。

---

### [BugFix][Router Replay] Capture Logical Experts with EPLB (#33013)
**SHA**: `13b842f` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/13b842f2714558abd96f260ab450d192ce687ffb)

**🎯 变更类型**：Bug 修复 / 功能增强  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
1. 将路由器的专家捕获职责从 `FusedMoE` 层迁移到 `BaseRouter`，新增 `capture_fn` 与 `set_capture_fn` 接口。  
2. `GPUModelRunner` 在初始化时统一绑定 `RoutedExpertsCapturer`，为每个 `BaseRouter` 注入捕获回调。  
3. 移除 `FusedMoE` 中的旧 `self.capture` 逻辑，并相应删掉相关 import。  
4. 新增单元测试验证：① 捕获在 EPLB 前后均能得到逻辑 ID；② 绑定过程在 `GPUModelRunner` 正确执行。

**🎯 影响范围**  
- `vllm/model_executor/layers/fused_moe/layer.py`（捕获属性删除）  
- `vllm/model_executor/layers/fused_moe/router/base_router.py`（新增 capture 接口）  
- `vllm/v1/worker/gpu_model_runner.py`（绑定实现）  
- 新增测试 `tests/model_executor/test_routed_experts_capture.py`

**💡 关注建议**  
- 确认 `enable_return_routed_experts` 为 True 时，`GPUModelRunner` 必然调用 `_bind_routed_experts_capturer`，否则捕获函数会保持 `None`；可在 `init_routed_experts_capturer` 前加入断言或日志。  
- 注意 `capture_fn` 现在在 EPLB 映射前被调用，若外部代码依赖映射后的 ID，需要自行在回调里再次映射。  
- 文档与示例应更新为 “使用 `router.set_capture_fn`” 而非直接访问 `layer.capture`，防止旧用法失效。  
- 运行全套 CI（包括 GPU 测试）确保多卡环境下回调不会因序列化/跨进程而失效。  

整体改动提升了捕获路径的可插拔性，降低了层之间的耦合，但需留意向后兼容性与配置初始化顺序。

---

### [fix][torch.compile] Fix cold-start compilation time increase by adding kv cache update to splitting ops (#33441)
**SHA**: `15f40b2` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/15f40b20aadf27af43bdae38bf4644b82e21634f)

**🎯 变更类型**：Bug 修复 / 功能增强  
**⚡ 重要程度**：🟡 中  
**📋 变更摘要**：  
1. 为了解决 VLLM‑compile 冷启动时因 KV‑cache 更新导致的编译时间激增，在 `CompilationConfig` 中把 `vllm::unified_kv_cache_update` 加入分割算子列表（仅在不使用 Inductor 图划分的情况下）。  
2. `split_graph` 逻辑新增“保持连续分割算子在同一子图” 的处理，防止因为相邻的 `attention`、`sqrt` 等算子被错误拆分成多个子图。  
3. 补充两组单元测试：① 验证冷启动时 AOT‑autograd 缓存命中/未命中次数（确保只对 3 种子图重新编译）；② 验证相邻分割算子会被合并为同一子图并保持前向计算相等。

**🎯 影响范围**：  
- `vllm/compilation/backends.py`（图划分实现）  
- `vllm/config/compilation.py`（默认分割算子配置）  
- 新增/修改的测试文件 `tests/compile/test_cold_start.py`, `tests/compile/test_graph_partition.py`  
- 任何使用 `CompilationMode.VLLM_COMPILE` 且未开启 Inductor 图划分的模型加载/推理流程。

**💡 关注建议**  
1. **功能验证**：在 CI 中跑完整的 compile 测试套件，确保加入 `unified_kv_cache_update` 不会意外把 KV‑cache 从 CUDAGraph 中剔除，导致运行时回退。  
2. **性能回归**：对比开启/关闭 `use_inductor_graph_partition` 两种模式下的冷启动编译时长与推理吞吐，确认新增分割不引入额外开销。  
3. **兼容性**：检查旧版自定义 `splitting_ops` 配置是否仍然生效，避免因默认追加导致用户配置冲突。  
4. **文档更新**：在 `vllm` 编译配置说明中标注 `unified_kv_cache_update` 只在非 Inductor 划分模式下自动添加的行为，以及其对 CUDAGraph 的副作用。  

总体来看，改动定位明确、测试覆盖充分，预计可显著降低冷启动编译时间。后续关注以上几点可进一步提升稳定性与可维护性。

---

### [Bugfix] Fix incompatibility between #33372 and #32863 (#33475)
**SHA**: `4cb59de` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/4cb59dea6a9b54cd3cae87ac69fb0a836615f196)

**🎯 变更类型**：Bug 修复  
**⚡ 重要程度**：🟡 中  
**📋 变更摘要**：本次提交删除了在多个渲染器中强制 `return_dict=False` 的代码，并在 `params.get_apply_chat_template_kwargs` 中统一加入 `return_dict=False`，以解决 #33372 与 #32863 之间的兼容性冲突。  

**🎯 影响范围**：`vllm/renderers/deepseek_v32.py`、`vllm/renderers/grok2.py`、`vllm/renderers/hf.py`、`vllm/renderers/params.py` 四个核心渲染模块。  

**💡 关注建议**  
1. **行为变化**：删除局部 `kwargs["return_dict"] = False` 后，若调用方未显式传入 `return_dict`，`tokenizer.apply_chat_template` 将恢复默认返回类型（通常为字典）。请确认后续代码（如 `prompt_raw` 的后处理）能够兼容字典返回或已统一通过 `params` 强制返回字符串。  
2. **一致性**：统一在 `Params.get_apply_chat_template_kwargs` 中设置 `return_dict=False`，确保所有渲染路径得到相同的返回格式，避免因不同路径返回值类型不一致导致的隐藏错误。  
3. **测试覆盖**：添加或更新针对上述渲染器的单元测试，验证在不同 tokenizer 配置下（默认返回 dict 与返回 string）渲染结果仍然正确。尤其关注异步 `render_messages_async` 的兼容性。  
4. **文档更新**：若 `return_dict` 参数的默认行为对用户有影响，建议在文档中说明渲染器现在默认强制返回字符串，并提供自定义 `return_dict` 的方式。  

总体而言，此次修改通过集中管理 `return_dict` 参数，消除了历史代码中的冲突，提升了渲染层的可维护性。但请务必确保所有下游逻辑已适配统一的返回格式，并通过测试验证。

---

### [ez] Add structured torch.compile logs (#33213)
**SHA**: `608b556` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/608b556507acbb15c88d4c119414fc8364627472)

**🎯 变更类型**：功能增强（为 vLLM 的 piece‑wise torch.compile 引入结构化日志）  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
1. 在 `vllm/compilation/backends.py` 与 `vllm/compilation/piecewise_backend.py` 中加入对 `torch._logging._internal.trace_structured` 的调用，实现以下日志：  
   - 编译配置（artifact）  
   - 顶层分片图（graph_dump）  
   - 每个子模块的图 dump（仅一次）  
   - 每次子图编译起始信息（artifact `vllm_piecewise_compile_start`）  
2. 新增单元测试 `tests/compile/test_structured_logging.py`，使用 `unittest.mock.patch` 捕获 `trace_structured` 调用并校验日志的数量、名称及内容。  

**🎯 影响范围**  
- **编译后端**：`VllmBackend` 与 `PiecewiseBackend`，所有依赖 `torch.compile` 的路径都会多一次结构化日志的产生。  
- **配置模块**：`CompilationConfig` 相关字段被序列化为 JSON，新增对 `pass_config` 中布尔字段的动态遍历。  
- **测试套件**：新增 121 行测试，仅在 CUDA 环境下运行。  

**💡 关注建议**  
1. **性能与日志开销**：`trace_structured` 本身是轻量的，但在子图首次编译时会额外执行一次 `graph.print_readable`，确保在大模型或频繁重新编译的场景下不会显著拖慢启动时间。可考虑在 CI/生产环境通过环境变量关闭（如 `TORCH_LOG=0`）。  
2. **兼容性**：`torch._logging._internal` 仅在 PyTorch 2.3+ 中稳定，若项目仍支持旧版 PyTorch，需添加导入降级或包装层，以防 `ImportError`。  
3. **日志消费**：目前所有 payload 均为 JSON 串，建议在文档中说明对应的 `tlparse` 解析字段，避免 downstream 工具因字段变更而失效。  
4. **测试环境**：新增测试依赖 `regex`，确认在项目 `requirements.txt` 中已声明；此外，CI 需要 CUDA GPU 支持，若没有可在 `python -m pytest -k test_vllm_structured_logging_artifacts -skip` 方式跳过。  
5. **代码可维护性**：`_log_compilation_config` 与 `_log_compile_start` 两个辅助函数在后续若增删配置字段，需要同步更新对应的 JSON 构造逻辑，建议抽象为统一的 “to_dict” 方法，降低维护成本。  

总体而言，此次改动为 vLLM 引入了对 TORCH_TRACE/tlparse 的可观测性，便于定位编译阶段的瓶颈和验证分片策略。只要注意上述兼容性与日志开销，即可安全合并。

---

### [Kernel] [Helion] [3/N] Helion kernel registry (#33203)
**SHA**: `d5c41db` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/d5c41db35b33d348bc8b913eab6d11a20a64f168)

**🎯 变更类型**：功能增强（新增 Helion Kernel Registry）  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
- 在 `vllm/kernels/helion/register.py` 中实现全局注册表 `_REGISTERED_KERNELS`，并提供 `register_kernel`、`get_registered_kernels`、`get_kernel_by_name` 三个公开 API。  
- 为 `register_kernel` 添加 overload，支持 **裸装饰器** 与 **带参数装饰器** 两种调用方式，并在未提供 `fake_impl` 时自动生成。  
- 改进 `ConfiguredHelionKernel` 对 `config_picker` 为 `None` 的容错处理。  
- 在 `vllm/kernels/helion/__init__.py` 重新导出新 API。  
- 补充完整单元测试，验证注册、重复检查、警告、配置传递等行为。

**🎯 影响范围**  
- 核心模块：`vllm/kernels/helion/register.py`、`vllm/kernels/helion/__init__.py`、相关测试。  
- 所有直接或间接使用 `register_kernel` 的用户代码（包括第三方插件）将看到返回值从 “装饰后函数” 改为 `HelionKernelWrapper` 实例。  

**💡 关注建议**  
1. **兼容性检查**：若外部代码依赖旧装饰器返回原函数（例如 `@register_kernel` 立即调用），需改为使用返回的 wrapper 或通过 `wrapper.raw_kernel_func` 访问原实现。可在文档中注明破坏性改动。  
2. **线程安全**：全局字典目前没有锁保护，若在多线程环境下并发注册可能产生 race，建议在未来加入 `threading.Lock` 或使用 `collections.defaultdict` 的原子操作。  
3. **注册表清理**：测试中显式 `clear()`，生产环境若需要热更新/卸载插件，提供 `unregister_kernel(name)` 会更友好。  
4. **日志级别**：现在使用 `logger.info`、`logger.debug`，在高并发部署时可考虑降低为 `debug`，避免不必要的 I/O。  
5. **类型提示**：已加入 `overload` 与 `cast`，确保 `mypy` 能正确推断；建议在 `pyproject.toml` 中把 `strict = true`，防止遗漏新的类型错误。  

总体而言，此次改动为 Helion Kernel 体系提供了统一、可查询的注册机制，增强了可维护性和可测试性。只要在升级指南中说明返回值变化并做好兼容性验证，风险可控。

---

### Add support for Mistral Large 3 inference with Flashinfer MoE (#33174)
**SHA**: `f0bca83` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/f0bca83ee4b6aa6d63da66d37dd69929bdcfc1fe)

⚠️ LLM分析失败（已重试3次）: API请求失败: HTTPSConnectionPool(host='integrate.api.nvidia.com', port=443): Read timed out. (read timeout=30)

*暂无分析*

---

### [Bugfix] Fix `Qwen3ASR` language asr tag in output  (#33410)
**SHA**: `e77f162` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/e77f162cf59d6e132c8b6449dc98b410b347a5b5)

**🎯 变更类型**：Bug 修复  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
- 在 `speech_to_text` 流程中加入对模型原始输出的统一后处理，专门剔除 Qwen3‑ASR 返回的 `language <asr_text>` 前缀。  
- 为 `ModelInterface` 增加 `post_process_output` 抽象实现，默认返回原文；在 `qwen3_asr.py` 中实现具体的标签剥离逻辑。  
- 注释说明流式返回时需对可能被切分的标签做缓冲处理。

**🎯 影响范围**  
- `vllm/entrypoints/openai/translations/speech_to_text.py`（ASR 输出生成）  
- `vllm/model_executor/models/interfaces.py`（模型通用接口）  
- `vllm/model_executor/models/qwen3_asr.py`（Qwen3‑ASR 专用实现）  

**💡 关注建议**  
1. **兼容性**：新抽象方法默认不影响其他模型，确保未实现 `post_process_output` 的模型仍返回原始文本。  
2. **测试**：新增单元测试，覆盖带/不带 `<asr_text>` 标签的输出、空字符串以及语言前缀的各种组合；加入流式模式下的边界情况（标签跨块）验证。  
3. **文档**：在模型使用说明中标明 Qwen3‑ASR 会自动移除语言标签，若需要保留可自行调用 `model_cls.post_process_output` 前的原始文本。  
4. **后续优化**：实现流式缓冲逻辑，确保在分块时能够完整检测并剔除 `<asr_text>` 前缀，避免出现半截标签。  

以上修改提升了 ASR 输出的可读性，避免了用户端额外的文本清洗工作。请在合并前通过完整的回归套件验证，尤其关注多模态音频转写的端到端流程。

---

### [Kernel] [Helion] [2/N] Helion kernel wrapper (#32964)
**SHA**: `8ecd213` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/8ecd213c0b63d012db3b003120d7d78edafe785f)

**🎯 变更类型**：功能增强（为 Helion 提供预调配置的统一包装层）  
**⚡ 重要程度**：🟡 中  
**📋 变更摘要**：新增 `vllm/kernels/helion/register.py` 实现 `HelionKernelWrapper`、`ConfiguredHelionKernel` 与 `PresetConfigSearch`，并在 `__init__.py` 中导出。配套添加单元测试、GPU 名称工具以及对 `helion.Settings` 的校验逻辑，支持在运行时直接使用预先调好的配置而不进行 autotune。  

**🎯 影响范围**  
- **vllm.kernels.helion**：核心注册流程、配置管理、工具函数全线改动。  
- **torch.ops.vllm_helion**：新建自定义 Op 的注册入口，对外可直接调用。  
- **测试套件**：新增两套针对注册器与工具函数的 600 行测试，提升覆盖率。  

**💡 关注建议**  
1. **异常路径**：`ConfiguredHelionKernel._create_key_computer` 在 `config_picker` 返回 `None` 时回退到 `"default"`，但若 `default` 不存在会返回 `None` 并随后抛 `ValueError`。建议在报错信息中明确告知 “default config missing”。  
2. **静态形状默认值**：`_create_decorated_kernel` 将 `static_shapes` 设为 `False` 除非用户显式设为 `True`。若用户在 `helion.Settings` 中未声明该字段，仍会被覆盖，文档需说明此行为。  
3. **线程安全**：`HelionKernelWrapper.get_configured_op` 在并发首次注册时可能出现竞争（多个线程同时检测 `torch.ops.vllm_helion` 不存在），建议加锁或使用 `torch.ops.vllm_helion.__dict__` 的原子检查。  
4. **内部依赖**：直接 `import helion` 会在未安装时抛 `ImportError`，但在 `__init__.py` 已导入 `register`，导致 `import vllm.kernels.helion` 仍会失败。若希望在未装 Helion 时仅禁用相关功能，可改为懒加载或捕获异常。  
5. **测试 Mock**：大量使用 `patch("vllm.kernels.helion.register.helion.kernel")`。若未来 `helion` 包内部装饰器签名变更，测试可能失效。建议抽象为内部工厂函数，便于替换。  

总体来看，改动为 Helium‑Back‑end 引入了可插拔的预调配置机制，提升了 vLLM 在特定 GPU 平台上的启动性能。只要注意上述异常与并发细节，后续可平稳上线。

---

### [Streaming -> Realtime] Rename all voxtral related classes, fn, files (#33415)
**SHA**: `15e0bb9` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/15e0bb9c428e08ef74e067da2b8b3f4765e3cd52)

**🎯 变更类型**：重构 / 其他  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
本次提交将原有的 *VoxtralStreaming* 相关实现统一改名为 **VoxtralRealtime**，包括类、函数、文件以及文档中的全部引用。核心改动涉及 `vllm/model_executor/models/voxtral_realtime.py` 中的处理器、生成器类及其内部错误提示文字；同时更新了模型注册表、示例脚本、测试用例和文档链接。

**🎯 影响范围**  
- `vllm/model_executor/models/registry.py`、`tests/models/registry.py` 中的模型键名  
- `vllm/model_executor/models/voxtral_realtime.py`（类名、注释、错误信息）  
- `vllm/transformers_utils/configs/mistral.py`（架构映射）  
- 文档 `docs/serving/openai_compatible_server.md` 与示例脚本链接  
- 相关单元测试 `test_voxtral_realtime_*.py`  

**💡 关注建议**  
1. **兼容性**：若已有下游项目引用 `VoxtralStreaming*`，应提供迁移指引或临时别名（如在 `__init__` 中保留旧名称的导入），避免突发性 `ImportError`。  
2. **文档同步**：确认所有公开文档、README 以及示例仓库链接已更新为新文件路径，防止用户仍搜索旧名导致困惑。  
3. **测试覆盖**：确认跳过的 `VoxtralStreaming` 测试已全部改为 `VoxtralRealtime`，且 CI 中未残留对旧名称的硬编码。  
4. **错误信息**：所有改为 “realtime” 的提示已更新，请再次检索项目中是否还有残留的 “streaming” 字样，以保持信息一致。  
5. **发布说明**：在下一个 Release Note 中明确标注 “VoxtralStreaming → VoxtralRealtime”，并给出迁移步骤，以帮助用户平滑升级。  

总体而言，改名本身不影响功能实现，但对外 API 与文档的完整同步至关重要，请在正式发布前完成上述检查。

---

### [UX] Use gguf `repo_id:quant_type` syntax for examples and docs (#33371)
**SHA**: `29fba76` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/29fba76781441ff38796eb0c189bbdd0497dc58c)

**🎯 变更类型**：功能增强  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
1. 文档与示例统一使用 `repo_id:quant_type` 语法，直接从 HuggingFace 拉取 GGUF 量化模型。  
2. `gguf_utils.is_valid_gguf_quant_type` 新增对 GGUF 常用后缀（`_M/_S/_L/_XL/_XS/_XXS`）的识别，支持如 `Q4_K_M`、`Q3_K_S` 等扩展命名。  
3. `is_remote_gguf` 与 `split_remote_gguf` 的错误提示同步更新。  
4. 增添单元测试覆盖基础量化类型、扩展后缀以及分割函数的边界情况。  

**🎯 影响范围**  
- `vllm/transformers_utils/gguf_utils.py`（核心判定/解析逻辑）  
- 文档 `docs/features/quantization/gguf.md`、示例 `examples/offline_inference/basic/README.md`  
- 单元测试 `tests/transformers_utils/test_utils.py`  

**💡 关注建议**  
1. **向后兼容**：现有 `repo_id:quant_type` 已支持原始 GGML 类型，新增后缀不会影响旧用法，保持兼容性。  
2. **后缀列表维护**：后缀集合 `_GGUF_QUANT_SUFFIXES` 写死在代码中，如后续出现新后缀需同步更新，否则 `is_valid_gguf_quant_type` 会误报无效。建议抽离为常量或从 `ggml` 库动态读取（若有提供）。  
3. **错误信息**：`split_remote_gguf` 的异常信息已补充“Extended suffixes also supported”，但在生产环境抛出时仍会返回完整列表，可能泄露实现细节。若不希望暴露，可改为更简洁提示。  
4. **测试覆盖**：新加入的测试已覆盖常见后缀，但未覆盖非法组合（如 `Q4_KM`、`Q4_K_`）以及大小写不一致的情况，建议再补充几例，以防止误判。  
5. **文档同步**：示例已改用 `unsloth/Qwen3-0.6B-GGUF:Q4_K_M`，请确保 CI 中的 README 渲染与实际模型 ID 对应；若模型名称变动，文档需及时更新。  

总体而言，此次改动提升了用户直接通过 HuggingFace 加载 GGUF 量化模型的体验，代码实现简洁且已配套单元测试，风险较低。后续关注后缀集合的维护以及异常信息的用户友好度即可。

---

### [ModelRunner V2] Fix spec decoding + logprobs (#33391)
**SHA**: `876a16f` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/876a16f4fb092a7954ce2c8db177943c55adb6a1)

**🎯 变更类型**：功能增强/Bug 修复  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
1. 为 `LogprobsTensors` 新增 `cu_num_generated_tokens` 字段，统一跨模块的 token 数信息。  
2. 修正 `ModelRunner V2` 在采样阶段对 logits、idx_mapping 与 cu‑num‑logits 参数的解码与传递，防止 shape 不匹配导致的错误。  
3. 相应更新 `sampler.compute_topk_logprobs`、`sampler.__call__`、`gpu_model_runner._get_prompt_logprobs_dict` 等调用签名，并在 `tests/v1/engine/test_output_processor.py` 中加入相应断言。  

**🎯 影响范围**  
- `vllm/v1/engine/logprobs.py`  
- `vllm/v1/outputs.py`（LogprobsTensors）  
- `vllm/v1/worker/gpu/model_runner.py`、`gpu_model_runner.py`  
- `vllm/v1/worker/gpu/sample/*`（sampler、logprob）  
- 单元测试 `tests/v1/engine/test_output_processor.py`  

**💡 关注建议**  
- 确认 `filter` 在携带 `cu_num_generated_tokens` 时会抛出明确异常，避免误用。  
- 检查所有外部调用（如旧版 `ModelRunner`）是否已同步更新参数签名，防止向后兼容性回退。  
- 运行完整的集成测试，尤其是多批次、变长 logits 场景，确保 `cu_num_logits` 正确映射到生成 token 数。  
- 更新文档和示例，说明 `LogprobsTensors.cu_num_generated_tokens` 的含义及使用限制。  
- 关注潜在的轻微额外内存开销（保存每请求的生成 token 长度列表），在大规模部署时评估其影响。

---

### [Deprecation] Deprecate `seed_everything` and `scatter_mm_placeholders` in v0.15 (#33362)
**SHA**: `010ec0c` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/010ec0c30ef1fa481fc69b8a5d2c205052d93607)

**🎯 变更类型**：功能增强 / 其他（API 弃用）  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
1. 在 `vllm/platforms/interface.py` 中将 `seed_everything` 标记为 **已弃用** 并全部删除实现，改为推荐使用 `vllm.utils.torch_utils.set_random_seed`。对应的 `random`、`numpy` 引入也被去掉。  
2. 在 `vllm/v1/worker/utils.py` 中同样将 `scatter_mm_placeholders` 与 `gather_mm_placeholders` 标记为 **已弃用** 并删除实现，提示将在 v0.15.0 后移除。  
3. `vllm/model_executor/models/voxtral.py` 删除了已注释的 transcript token ID cached properties，保持功能不变。

**🎯 影响范围**  
- `vllm.platforms.interface.Platform`（种子设置）  
- `vllm.v1.worker.utils`（多模态占位符 scatter/gather）  
- Voxtral 模型加载代码（虽未影响运行，但清理了废弃属性）  

**💡 关注建议**  
- 项目内部或用户代码若仍调用 `Platform.seed_everything`，请改为 `vllm.utils.torch_utils.set_random_seed(seed)` 并自行处理 `random` 与 `numpy` 的种子。  
- 若有自行实现的多模态占位符散布/收集逻辑，迁移至 `PromptUpdateDetails.is_embed` 的使用方式，或直接使用 `vllm.multimodal.processing` 中的新 API。  
- 删除的 `random`、`numpy` import 可能导致未显式引用时出现 `NameError`，检查相关模块的导入顺序。  
- 运行 CI/单元测试，确保未残留对已弃用函数的引用，防止在 v0.15 正式发布后出现兼容性错误。

---

### [Bugfix][ROCm] Fixing the skinny gemm dispatch logic from #32831 (#33366)
**SHA**: `31aedfe` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/31aedfe7d62adea6b5a7644f3c323f585b042ce3)

**🎯 变更类型**：Bug修复（ROCm skinny gemm 调度）  
**⚡ 重要程度**：🟡 中  
**📋 变更摘要**：  
1. 为了让 ROCm skinny gemm 正确处理非连续张量，去除了原先在 `_custom_ops.py` 中对输入强制 `contiguous()` 的包装，并在调用点加入了显式的 `is_contiguous()` 检查。  
2. 在 `tests/kernels/quantization/test_rocm_skinny_gemms.py` 中加入 `pad_weights_fp8` 辅助函数以及 `padded` 参数，覆盖了权重被 256‑byte 对齐后截断的情况，确保新调度在有/无填充两种情形下均通过。  
3. 对 `rocm_per_tensor_float_w8a8_scaled_mm_impl`、`rocm_unquantized_gemm_impl` 添加 `A.is_contiguous()`、`x.is_contiguous()` 断言，使得只有在满足连续性的前提下才走 skinny‑gemm 路径。

**🎯 影响范围**：  
- `vllm/_custom_ops.py`（所有 ROCm skinny gemm 接口）  
- `vllm/model_executor/layers/quantization/kernels/scaled_mm/rocm.py`（scaled mm 实现）  
- `vllm/model_executor/layers/utils.py`（unquantized gemm 调度）  
- 相关测试文件 `tests/kernels/quantization/test_rocm_skinny_gemms.py`

**💡 关注建议**：  
- 开发者在使用 ROCm skinny gemm 时仍需保证输入张量连续，否则会退回通用实现；如有必要可手动调用 `.contiguous()`。  
- 关注后续 PR 是否恢复对非连续张量的优化支持（如在 kernel 中加入 stride 处理），以免出现不必要的拷贝。  
- CI 已增添填充/不填充两套路径的验证，推荐在本地跑 `pytest -k rocm_skinny_gemms` 确认兼容性。  
- 若用户在 AMD GPU 上仍遇到性能回退，请检查 `torch.cuda.is_available()` 与 `torch.backends.cuda.is_built()` 是否正确指向 ROCm，并确认 `x.is_contiguous()` 为 True。

---

### [Kernel] [Helion] [1/N] Add Helion ConfigManager (#32740)
**SHA**: `6c1f9e4` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/6c1f9e4c1877cb5cb6a702f5813e606eaba35600)

**🎯 变更类型**：功能增强（新增 Helion 配置管理）  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
- 在 `vllm/kernels/helion` 目录新增 `config_manager.py`，实现 `ConfigSet`（内存配置集合）和全局单例 `ConfigManager`（JSON 文件级的加载、保存、目录管理）。  
- 为其编写了完整的单元测试（`tests/kernels/helion/test_config_manager.py`），覆盖创建、序列化、错误处理、单例行为等路径。  
- 在 `vllm/kernels/__init__.py` 与 `vllm/kernels/helion/__init__.py` 中加入相应的导出声明。  

**🎯 影响范围**  
- 新增的模块将被 `vllm.kernels.helion` 包直接导出，任何引用该子包的代码都会得到 `ConfigManager`/`ConfigSet`。  
- 依赖 `helion` 包的环境（通过 `has_helion` 检查），否则会在导入时抛 `ImportError`。  
- 单例实现会影响进程内的所有配置读取/写入，涉及 `base_dir` 参数的冲突检测。  

**💡 关注建议**  
1. **单例线程安全**：当前实现仅在单线程下保证唯一性，若有并发初始化（如在多线程或多进程环境）可能出现竞争；可考虑使用 `threading.Lock` 或 `multiprocessing` 方案防止 race。  
2. **异常信息统一**：`load_config_set` 在 JSON 解析错误后返回空 `ConfigSet`，但不会向调用方明确提示配置文件损坏；建议在日志级别之外抛出自定义异常，便于上层定位。  
3. **平台/键大小写**：`get_config_keys` 对平台使用 `platform.lower()`，但其它方法直接用原始字符串，可能导致不一致；统一大小写或在文档中约定平台名区分大小写。  
4. **路径默认位置**：默认 `configs` 目录位于源码目录下，发布的 wheel 中可能不可写；若用户在只读安装环境使用，`ensure_base_dir_exists` 会尝试创建目录失败。建议在文档中说明或提供环境变量覆盖默认路径。  
5. **测试依赖**：单元测试只在 `has_helion()` 为真时运行，CI 环境需确保 `helion` 已安装，否则这些覆盖率将缺失。  
6. **`to_dict` 实现**：内部使用 `json.loads(config.to_json())` 进行转换，若 `helion.Config` 将来改变返回格式，可能出现隐蔽错误；考虑直接访问属性或实现统一的序列化方法。  

总体来看，此次改动为 Helion 自定义 kernel 提供了结构清晰、易于维护的配置管理框架，代码风格与项目一致。关注上述细节后，可提升稳定性并避免潜在的运行时冲突。

---

### Support FP8 block quant for CompressedTensorsW8A16Fp8 (#33280)
**SHA**: `fd0e377` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/fd0e3772441a32e1c5948d62a8702417fac59979)

**🎯 变更类型**：功能增强  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
为 `CompressedTensorsW8A16Fp8` 添加了 FP8 **块量化（block‑quant）** 支持。`CompressedTensorsW8A16Fp8` 现在接受 `QuantizationArgs`，能够根据 `QuantizationStrategy.BLOCK` 创建块尺度参数、校验块尺寸并在加载后对权重进行块‑特定的转置/尺度处理。相关工具函数 (`create_fp8_weight_parameter`, `create_fp8_scale_parameter`, `process_fp8_weight_block_strategy` 等) 迁入 `fp8_utils` 并在 `prepare_fp8_layer_for_marlin` 中加入 `size_k_first` 控制。

**🎯 影响范围**  
- `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors.py`  
- `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a16_fp8.py`  
- `vllm/model_executor/layers/quantization/fp8.py`（去除冗余 `set_weight_attrs`）  
- `vllm/model_executor/layers/quantization/utils/fp8_utils.py`（新增块相关工具）  
- 相关参数类 `BlockQuantScaleParameter`、`replace_parameter`、`set_weight_attrs`  

**💡 关注建议**  
1. **兼容性检查**：确认旧模型（非块量化）在加载时仍走原路径，`is_static_input_scheme` 的默认值未被误改。  
2. **单元测试**：新增针对 `QuantizationStrategy.BLOCK` 的权重/尺度创建、块形状校验、`process_weights_after_loading` 的行为测试。  
3. **硬件依赖**：该特性仅在 Turing 及以上 GPU（SM75+）可用，需在文档和运行时检查中提示。  
4. **序列化/恢复**：确保 `weight_block_size`、块尺度等信息在模型保存/加载时完整保存。  
5. **性能验证**：对比块量化与通道/张量量化的吞吐与精度，验证 Marlin‑FP8 路径的正确性。  

总体而言，改动为 FP8 块量化铺路，代码结构更清晰，但需通过测试和文档同步确保稳定落地。

---

#### 🟢 低重要度变更 (21)

### [Doc] Update plugin deprecation notices (#33476)
**SHA**: `793af53` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/793af538a35a7277a2c9ca0ca5f51adb043b97f5)

**🎯 变更类型**：文档更新  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `docs/design/plugin_system.md` 中将 `seed_everything` 接口的弃用说明更新为已在 v0.16.0 移除，建议改用 `vllm.utils.torch_utils.set_random_seed`。

---

### support return prompt token ids in responses  (#33378)
**SHA**: `6f5e7cd` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/6f5e7cda57e973ccb7fca22862e107119515b33a)

**🎯 变更类型**：文档更新  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：在 `ResponsesRequest` 的字段描述中，精简了对返回消息支持的说明，去掉了仅限非后台和 gpt-oss 的限定措辞。整体文字更简洁，功能不变。

---

### [Misc] Replace deprecated interface seed_everything (#33474)
**SHA**: `68feb76` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/68feb76a6f89abd6a52116c99b9b39e538a9a9ac)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 CPU 基准测试脚本中，将已弃用的 `current_platform.seed_everything` 替换为统一的 `set_random_seed` 工具函数，保持随机种子设置的一致性。

---

### [perf] v1/spec_decode: skip softmax for all-greedy rejection sampling (#32852)
**SHA**: `8980001` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/8980001c9333c617f2bc3b2c60926b02434067a7)

**🎯 变更类型**：代码重构/性能优化  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `rejection_sampler.py` 中将对 target logits 的 softmax 计算推迟至必要时再执行，去除冗余的 softmax 步骤，降低计算开销并提升采样性能。

---

### [ROCM] Enable aiter attn backend for qwen3-next model (#32492)
**SHA**: `527bcd1` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/527bcd14d46595b2bde9e48f3d2f10879623b891)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 ROCM AITER FlashAttention 后端文档与实现中，将支持的 kernel block size 由仅 `MultipleOf(16)` 扩展为 `[16, 32]`，同步更新了表格说明。

---

### [BugFix] Add synchronize in CutlassW4A8LinearKernel to ensure data is ready for use. (#33078)
**SHA**: `f68e3ea` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/f68e3ea4e135c41b997d8226186509659b3d722c)

**🎯 变更类型**：代码重构 / BugFix  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：在 `CutlassW4A8LinearKernel` 中加入 `torch.cuda.synchronize()`，确保在量化权重转换后 GPU 数据已同步完毕，防止后续使用时出现未就绪的竞争问题。

---

### [CPU][Feat] Enable KleidiAI accelerated int4 dynamic quant with BF16 activations on Arm CPUs (#33122)
**SHA**: `1618e25` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/1618e2549281324100690db3bd8c5f497fa4fc3f)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `Dynamic4bitLinearKernel` 中加入对 Arm CPU 上 KleidiAI 加速的 int4 动态量化支持，允许使用 BF16（以及 Float32）激活，实现更高效的 4bit‑8bit 矩阵乘法。

---

### Add EAGLE3 support for AFMoE (#33111)
**SHA**: `f3888ac` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/f3888aca83edf5125c37d0983f1df94df27d9920)

**🎯 变更类型**：代码重构/特性新增  
**⚡ 重要程度**：🟢低  
**📋 摘要**：为 AFMoE 模型加入 EAGLE3 支持，新增 `SupportsEagle3` 接口、辅助隐藏层状态采集逻辑，并在配置校验中加入 `"afmoe"`。

---

### [Bugfix] Handle Asym W4A16 (ConchLinearKernel) for CT (#33200)
**SHA**: `73419ab` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/73419abfae97c0b797c7e1fe997913e9ebfeff68)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `conch.py` 中新增对 zero‑point 权重的解包与重排逻辑，支持 Asym W4A16 并在加载时正确更新元数据。  

---

### [Attention] Clarify comment explaining attn_logits +1 dimension (#33427)
**SHA**: `5b55c0b` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/5b55c0bea7410e60566e329b28070ce77269a322)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `triton_mla.py` 中将对 `+1` 的说明从模糊的猜测改为明确注释，解释该额外维度用于存放 Stage2 核心合并分片注意力时的 LogSumExp (LSE)。代码行为未变，仅提升可读性。

---

### [ROCm][CI] Force max_num_seqs=1 on ROCm In test_sharded_state_loader to reduce flakiness (#33277)
**SHA**: `6c64c41` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/6c64c41b4abfe10f725088afb1fe143d412dc118)

**🎯 变更类型**：测试修改  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：在 `test_sharded_state_loader` 中新增对 ROCm 平台的检测，若为 ROCm 则强制 `max_num_seqs=1`，以降低测试不稳定性。

---

### [Misc] offest -> offset in comments and variable names (#33444)
**SHA**: `a2ef06e` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/a2ef06e1b3a1c0dc4f7bf6587fbdcf1dad287c31)

**🎯 变更类型**：其他  
**⚡ 重要程度**：🟢低  
**📋 摘要**：将注释和变量中的拼写错误 “offest” 统一更正为 “offset”，涉及两个文件的少量行修改，代码功能保持不变。

---

### [BugFix] Fix whisper FA2 + full cudagraphs (#33360)
**SHA**: `0a3c71e` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/0a3c71e7e5f06507f64677f77d5fca84d9c7cc65)

**🎯 变更类型**：其他  
**⚡ 重要程度**：🟢低  
**📋 摘要**：修复 FlashAttention2 在 encoder‑decoder 模型使用 CUDA graphs 时的准确性问题，删除原有警告并在 CUDA graph 捕获阶段使用真实的 encoder 长度；为 `_get_encoder_seq_lens` 添加 `for_cudagraph_capture` 参数并相应传递。

---

### [Misc] Algin Qwen3-VL-embedding image example outputs with HF repo example (#33419)
**SHA**: `9df152b` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/9df152bbf6c27db737636960ae791546d65269fc)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `examples/pooling/embed/vision_embedding_offline.py` 中引入对 `qwen-vl-utils` 的可选依赖，实现图片自动缩放；为 `print_embeddings` 添加类型注解；在 `EngineArgs` 中加入 `mm_processor_kwargs` 以禁用内部自动缩放，从而对齐 HF 示例输出。

---

### [Bugfix] Fix typo in read_offset variable name (#33426)
**SHA**: `64a40a7` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/64a40a7ab4d0053830fae04c83763fa67f2183e6)

**🎯 变更类型**：其他  
**⚡ 重要程度**：🟢低  
**📋 摘要**：修复 `vllm/v1/engine/detokenizer.py` 中 `read_offest` 拼写错误，改为 `read_offset`，消除属性名不一致导致的潜在运行时错误。

---

### [CI][HPU]accelerate hpu test by skip python re-install and clean container name (#33286)
**SHA**: `2b46557` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/2b465570e6dd327e8422ef9c87e9b2b1454ceaed)

**🎯 变更类型**：配置调整  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 HPU CI 脚本中使用自定义镜像名和容器名，跳过 Python 重装，改为直接安装依赖并通过 `pip install -e .`，并在容器内运行离线推理示例以加速测试。

---

### Indicate compile mode in the benchmark results (#32990)
**SHA**: `9ca66ec` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/9ca66ecc1012418f6c6d865239835a0adfcae719)

**🎯 变更类型**：代码重构/功能补充  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：在性能基准脚本中加入编译模式与优化等级的采集并写入结果元数据；新增 `extract_field`、`use_compile` 工具函数，在 JSON 输出中加入 `compilation_config.mode`、`optimization_level` 与 `use_compile` 标记。

---

### [Quantization][ROCm] Fix MoE weight loading to be robust (Qwen3_MoE/Qwen3_next as example models) (#33173)
**SHA**: `f451b45` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/f451b4558b2bb42dafcdd19f7b5c0fc58194af5a)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 MoE 权重加载时增加 `loaded_weight.ndim > 0` 检查，防止对标量（0‑维）张量执行 `narrow`，提升在 Qwen3_MoE/Qwen3_next 等模型上的鲁棒性。

---

### fix QERL attention import path (#33432)
**SHA**: `3f96fcf` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/3f96fcf64631a100904bb189859f9aa80e8a75d5)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：修正 QERL 注意力模块的导入路径，将原先的 `vllm.attention.layer` 替换为 `vllm.model_executor.layers.attention`，确保正确引用实现并避免潜在的 ImportError。

---

### Fix encoder-decoder model disabling mm processor cache (#33236)
**SHA**: `67239c4` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/67239c4c4235ae668971625304390ed2078b4dd0)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：修正 `ModelConfig.__post_init__` 对 encoder‑decoder 模型的多模态处理器缓存禁用逻辑，避免直接修改实例属性导致后续设置失效；相应测试改为先创建模型后再手动赋值 `mm_processor_cache_gb`。

---

### [CI] Qwen3-ASR transcriptios tests (#33414)
**SHA**: `8ece607` | 🔗 [查看提交](https://github.com/vllm-project/vllm/commit/8ece60768f63989db7f26ef81c7f68635f70e081)

**🎯 变更类型**：测试修改  
**⚡ 重要程度**：🟢低  
**📋 摘要**：为转录接口新增对 Qwen3‑ASR 模型的参数化测试；调整断言文本以适配新模型输出；将 gemma 测试改为参数化并更新期望转录内容。

---

