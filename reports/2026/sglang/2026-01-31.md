# 每日更新报告（2026-01-31）

## sgl-project/sglang

| 提交时间 | 作者 | 提交信息 |
|----------|------|----------|
| 2026-01-31 23:01:29 | b8zhong | [Fix] Revert back to using CUTLASS `mm_fp4` backend (#17369) |
| 2026-01-31 20:22:31 | Mick | [diffusion] refactor: split component_loader into component-wise files (#17820) |
| 2026-01-31 17:01:55 | Lianmin Zheng | [Auto Sync] Update linear.py to assert shapes (20260130) (#17966) |
| 2026-01-31 17:00:40 | Lianmin Zheng | Add launch_command assignment in crash dump (#17967) |
| 2026-01-31 15:56:26 | b8zhong | [Fix] Triton TP MoE Dpsk V3/Qwen3 Coder with SwapAB (#17965) |
| 2026-01-31 14:30:11 | Zheng Wengang | [EPD][Perf] parallelize ZMQ send for encode server (#16487) |
| 2026-01-31 13:59:00 | jeff | Fix OOM in DeepSeek weight loading by deferring dict(weights) materialization (#17744) |
| 2026-01-31 13:42:21 | Yifan Cui | Reduce topk kernel shared memory from 128KB to 32KB for better occupancy (#17747) |
| 2026-01-31 13:09:23 | Hudson Xing | add reasoning_tokens usage test for tool call (#18022) |
| 2026-01-31 12:42:11 | husf | 【docs】【NPU】Update Expert Parallelism docs for Ascend NPU (#17940) |
| 2026-01-31 12:33:32 | R0CKSTAR | Fix .gitignore may ignore files like core_attention.py (#18021) |
| 2026-01-31 12:01:52 | Mohammad Miadh Angkad | Fix cuBLAS >=12.9 detection for cu12/cu13 package naming (#17766) |
| 2026-01-31 11:47:27 | Xiaoyu Zhang | [Diffusion] Fix FLUX.1-schnell time embedding argument mismatch (#17988) |
| 2026-01-31 11:12:07 | kk | Add ROCm + Mori docker build instructions in rocm.Dockerfile (#18018) |
| 2026-01-31 11:06:58 | 22dimensions | [NPU] fix sgl-kernel-npu package url error in npu.Dockerfile (#18017) |
| 2026-01-31 10:10:23 | Tiance Wang | doc update for CANN version (#18014) |
| 2026-01-31 09:09:13 | Bi Xue | [sglang] fix mm token padded value overlap with text token id (#17781) |
| 2026-01-31 08:49:38 | JiaruiChang5268 | [NPU] support llama-3.2-11B-vision-instruct mode for NPU (#17492) |
| 2026-01-31 03:57:42 | Siyuan Chen | [BugFix] Fix server crashes when req.grammar and ngram spec are enabled (#17585) |
| 2026-01-31 01:02:12 | Sam (Kesen Li) | Optimize GDN decode for Qwen3 Next (#17094) |

### 📊 统计摘要
> 本日共 20 个提交 | 🔴高 1 | 🟡中 3 | 🟢低 16
## 📋 目录

- [sgl-project/sglang](#sgl-project-sglang)
  - [📊 统计摘要](#-统计摘要)
  - [🔴 高重要度变更 (1)](#-🔴-高重要度变更-1)
    - [[diffusion] refactor: split component_loader into compone...](#1a006c2)
  - [🟡 中重要度变更 (3)](#-🟡-中重要度变更-3)
    - [[Fix] Triton TP MoE Dpsk V3/Qwen3 Coder with SwapAB (#17965)](#22498e1)
    - [[NPU] support llama-3.2-11B-vision-instruct mode for NPU ...](#e86476a)
    - [[BugFix] Fix server crashes when req.grammar and ngram sp...](#578b119)
  - [🟢 低重要度变更 (16)](#-🟢-低重要度变更-16)
    - [[Fix] Revert back to using CUTLASS `mm_fp4` backend (#17369)](#ef134d4)
    - [[Auto Sync] Update linear.py to assert shapes (20260130) ...](#7412ceb)
    - [Add launch_command assignment in crash dump (#17967)](#0e18460)
    - [[EPD][Perf] parallelize ZMQ send for encode server (#16487)](#a4df95c)
    - [Fix OOM in DeepSeek weight loading by deferring dict(weig...](#04efd03)
    - [Reduce topk kernel shared memory from 128KB to 32KB for b...](#45fe51a)
    - [add reasoning_tokens usage test for tool call (#18022)](#c72bf50)
    - [【docs】【NPU】Update Expert Parallelism docs for Ascend NPU ...](#c52578c)
    - [Fix .gitignore may ignore files like core_attention.py (#...](#dc77def)
    - [Fix cuBLAS >=12.9 detection for cu12/cu13 package naming ...](#d0d9cec)
    - [[Diffusion] Fix FLUX.1-schnell time embedding argument mi...](#22aad4e)
    - [Add ROCm + Mori docker build instructions in rocm.Dockerf...](#ec76c39)
    - [[NPU] fix sgl-kernel-npu package url error in npu.Dockerf...](#ee3058c)
    - [doc update for CANN version (#18014)](#f6a4ff7)
    - [[sglang] fix mm token padded value overlap with text toke...](#5d00150)
    - [Optimize GDN decode for Qwen3 Next (#17094)](#81449b4)
#### 🔴 高重要度变更 (1)

### [diffusion] refactor: split component_loader into component-wise files (#17820)
**SHA**: `1a006c2` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/1a006c2a0dea4856cdbc907cc96fade370f33b35)

**🎯 变更类型**：重构 / 架构变更  
**⚡ 重要程度**：🔴高  

**📋 变更摘要**  
- 将原本臃肿的 `component_loader.py` 拆分为多个专职文件（adapter、bridge、vae、scheduler、transformer、text_encoder、image_encoder、vl_encoder 等），每个文件实现对应组件的加载逻辑。  
- 引入统一的 **ComponentLoader** 基类，并通过 `ComponentLoader.for_component_type()` 动态发现、注册、实例化对应的子类。  
- 新增 `loader/utils.py`，提供通用工具（`skip_init_modules`, `_list_safetensors_files`, `_clean_hf_config_inplace`, `component_name_to_loader_cls` 等），并实现 **lazy‑registration**（`_ensure_loaders_registered`）以自动导入同目录下的 loader 子模块。  
- 更新 `PipelineComponentLoader` 接口为 `load_component`，并相应修改 `ComposedPipelineBase.load_modules` 中的变量名与调用方式。  
- 所有已有的加载路径（VAE、Transformer、Adapter、Bridge、Scheduler、Vision‑Language Encoder）迁移到独立文件，保持原有功能不变。  

**🎯 影响范围**  
- **加载子系统**：`sglang.multimodal_gen.runtime.loader` 包下的全部组件。  
- **管线初始化**：`sglang.multimodal_gen.runtime.pipelines_core.composed_pipeline_base`（模块加载）。  
- **若干 stage 实现**：`decoding.py`（VAE）与 `denoising.py`（Transformer）依赖新文件路径。  
- **工具函数**：`utils.py` 中的 `skip_init_modules`、`_list_safetensors_files`、`get_memory_usage_of_component` 被多个 loader 复用。  

---

## 🔍 技术洞察  

### 1. 架构影响
| 维度 | 正面影响 | 负面/注意事项 |
|------|----------|----------------|
| **模块化** | 每种模型组件的加载逻辑独立，代码行数下降（单文件 70‑120 行），易于维护、添加新组件（只需新增文件并声明 `component_names`）。 | 需要保证 **所有子类在首次使用前已被导入**，否则 `component_name_to_loader_cls` 为空导致回退到 `GenericComponentLoader`（性能/功能回退）。 |
| **注册机制** | `ComponentLoader.__init_subclass__` 自动把 `component_names` → loader class 注册到全局字典，避免手动维护映射表。 | 全局字典是 **mutable的单例**，在多进程/多线程环境下首次并发调用可能出现 race condition（两个进程同时尝试注册），虽然概率低，但在分布式启动时应注意。 |
| **向后兼容** | 保留了 `ComponentLoader.for_component_type` 的签名，只是改名为 “component”。 | 旧代码仍可能调用已删除的 `ComponentLoader.for_module_type`，或直接 import 旧类路径（如 `from .component_loader import VAELoader`），会触发 `ImportError`。需要在全项目搜索并更新。 |
| **加载路径统一** | `PipelineComponentLoader.load_component` 统一日志与错误处理，返回 `(module, memory_usage)` 与原实现保持一致。 | `load_modules` 中的变量名改为 `loaded_components`，若其它代码仍使用旧 `components` 变量会出现未定义错误。 |

### 2. 性能影响
- **加载时间**：新增的 **lazy import** (`_ensure_loaders_registered`) 在第一次调用 `for_component_type` 时遍历目录并 `importlib.import_module`，相当于一次额外的 **数十毫秒** I/O+编译，之后缓存生效，对实际推理性能无影响。  
- **内存占用**：拆分文件本身不改变模型权重加载方式；`skip_init_modules` 仍在 `utils.py` 中实现，行为保持不变。  
- **并行/分布式**：FSDP、HS‑DP 等加载路径保持原有实现，未被改动。  

### 3. 安全考虑
| 风险 | 描述 | 缓解 |
|------|------|------|
| **动态导入自定义模型代码** (`importlib.util.spec_from_file_location`) | 对 VAE、Bridge、Vocoder 等支持 `auto_map` 的自定义类时，会直接执行模型仓库中的 Python 脚本。若模型来源不可信，可能执行恶意代码。 | 与原实现相同；建议在部署环境仅允许受信任的模型目录，或在容器中使用只读挂载 + `PYTHONPATH` 限制。 |
| **路径遍历** (`_list_safetensors_files`) | 使用 `glob(os.path.join(model_path, "*.safetensors"))`，若 `model_path` 被注入恶意路径（如 `../../etc/passwd`）可能读取非模型文件。 | `ServerArgs` 中的 `model_path` 通常来源于配置文件或 CLI，需对其做绝对路径校验或限制在预设根目录。 |
| **全局注册表泄露** (`component_name_to_loader_cls`) | 任何代码都可修改该字典，潜在的 **供应链攻击**（恶意插件注入新的 loader）。 | 在生产环境可将字典设为 `MappingProxyType` 防止运行时修改，或在初始化后删除 `__setitem__`。 |

### 4. 可维护性与可扩展性
- **新增组件**：只需实现子类、声明 `component_names = ["my_component"]`、`expected_library = "diffusers"`，并放在 `loader/` 目录。无需改动中心映射表。  
- **文档/IDE 支持**：`ComponentLoader.for_component_type` 通过 **type hints**（返回 `ComponentLoader`）使 IDE 能提示可用子类。  
- **测试**：原有的 `loader.*` 单元测试需要更新导入路径；推荐为每个子类新增 **smoke test**（仅实例化并调用 `load_customized` 的异常分支），确保注册表正确。  

---

## ⚠️ 潜在风险  

| 风险点 | 可能后果 | 触发条件 | 建议的防御措施 |
|--------|----------|----------|----------------|
| **未能自动注册子类** | `ComponentLoader.for_component_type` 回退到 `GenericComponentLoader`，导致加载的模型使用 **原始 Diffusers/Transformers** 实现，性能下降或不兼容自定义权重。 | 首次调用发生在子进程未执行 `ComponentLoader._ensure_loaders_registered()`（如在 `fork` 后直接调用）。 | 在每个进程启动入口（如 `main.py`）显式调用 `ComponentLoader._ensure_loaders_registered()`；或将注册过程放入 `__init__` 模块级执行。 |
| **循环导入 / 导入顺序错误** | `ImportError` 或属性未定义（如 `ComponentLoader` 未定义的 `target_device`）。 | 某子加载器在导入时依赖另一个子加载器且两者相互 import。 | 保持每个子加载器仅依赖公共模块 (`component_loader

---

#### 🟡 中重要度变更 (3)

### [Fix] Triton TP MoE Dpsk V3/Qwen3 Coder with SwapAB (#17965)
**SHA**: `22498e1` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/22498e10c06a281a46e707fcb0cf3a843757363d)

**🎯 变更类型**：功能增强 / Bug 修复  
**⚡ 重要程度**：🟡 中  

**📋 变更摘要**  
1. 为 Triton‑TP MoE 的基准脚本 `benchmark/kernels/fused_moe_triton/tuning_fused_moe_triton_sep.py` 新增 `ep_size` 参数，并在 `prepare` 阶段对 `topk_ids` 进行 **全局‑>局部** 映射，实现对 Expert Parallel（EP）的支持。  
2. 将 `ep_size` 参数向上传递至 `benchmark`、`tune`、`cmp_configs`、`main` 等入口函数，确保 CLI 可控制。  
3. 新增多套针对 NVIDIA H200（fp8_w8a8）在不同 (E,N) 组合和 block‑shape 下的调优配置 JSON（正向与 down‑sampling 版），丰富硬件‑模型匹配库。  
4. 在 `fused_moe_triton_kernels.py` 中：  
   - 移除 `get_device_name` 依赖，改为直接检测 **SM90** 支持；  
   - 引入 `is_batch_invariant_mode_enabled()`，在 Batch‑Invariant 模式下关闭 `swap_ab`；  
   - 简化 `should_enable_swap_ab` 的判定逻辑。  

**🎯 影响范围**  
- **benchmark / tuning**：所有调用 `tuning_fused_moe_triton_sep.py` 的实验脚本和 CI 基准。  
- **MoE Triton kernel**：`swap_ab` 的开启/关闭策略，影响 SM90 系列 GPU（H20、H100、H200 等）的执行路径。  
- **配置库**：`sglang/srt/layers/moe/fused_moe_triton/configs/...` 新增约 540 条配置条目，供 `fused_moe_triton` 自动调优使用。  
- **运行时**：在开启 EP（`ep_size>1`）时，需要保证 `topk_ids` 的整数除法不产生精度误差；默认行为保持向后兼容。  

**💡 关注建议**  

1. **功能验证**  
   - 为 `ep_size` 添加单元测试：在 `ep_size=2,4` 时检查 `topk_ids` 是否被正确映射且不会越界。  
   - 在不同 GPU（SM90 与非 SM90）上跑一次基准，确认 `swap_ab` 在 Batch‑Invariant 模式下被禁用，防止意外性能回退。  

2. **文档与 CLI**  
   - 更新 `benchmark`/`tune` 脚本的 `argparse`，暴露 `--ep-size` 参数并在 README 中说明何时需要使用。  
   - 在配置文件目录的 `README` 中标注新加入的 H200‑fp8 配置的适用场景（如 Qwen3‑Coder、DPSK‑V3）。  

3. **兼容性 & 回滚**  
   - `should_enable_swap_ab` 现在仅检查 `is_sm90_supported()` 与块尺寸，若后续出现特例（如某些 SM90 GPU 对 `swap_ab` 有副作用），请保留 `--disable-swap-ab` 之类的环境开关。  
   - 确认 `is_batch_invariant_mode_enabled()` 的默认值为 `False`，避免在未显式开启时误关闭 `swap_ab`。  

4. **性能回归监测**  
   - 将新增的 JSON 配置加入自动调优测试矩阵，确保在 H200 上的 fp8‑w8a8 能够达到预期的吞吐提升。  
   - 对比开启/关闭 `swap_ab` 的核时延，确保新判定逻辑未引入不必要的延迟。  

总体来看，本次改动为 Triton‑TP MoE 引入 EP 支持并优化了 SM90 GPU 的 kernel 选择，提升了跨硬件的适配能力。只要在 EP 与非 EP 场景下做充分的回归测试，即可安全合入主线。

---

### [NPU] support llama-3.2-11B-vision-instruct mode for NPU (#17492)
**SHA**: `e86476a` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/e86476acfc525d7c378efbeab2321d5bc65b7899)

**🎯 变更类型**：功能增强  
**⚡ 重要程度**：🟡 中  
**📋 变更摘要**：为 NPU（Ascend）后端新增对 Llama‑3.2‑11B‑Vision‑Instruct 的跨模态（vision‑cross‑attention）支持。实现了 `AscendTorchNativeAttnBackend`，在 `ascend_backend.py` 中改用该实现并完善 kv‑cache、跨注意力以及 encoder‑lens 的处理；同时修正了线性层 bias 的初始化方式并加入对应的 CI 测试。

**🎯 影响范围**  
- `python/sglang/srt/hardware_backend/npu/attention/ascend_backend.py`（注意力调度逻辑）  
- 新增 `ascend_torch_native_backend.py`（基于 PyTorch 原生 SDPA）  
- `python/sglang/srt/layers/linear.py`（bias 初始化）  
- NPU CI 测试目录 `test/registered/ascend/vlm_models/`  

**💡 关注建议**  
1. **性能验证**：新实现在循环中逐条请求仍有潜在慢点，建议在 CI 中加入 `torch.compile`/批量化的基准，确认相较于原先 NPU flash attention 的吞吐提升或不降。  
2. **跨注意力正确性**：新增 `encoder_lens` 与 `is_cross_attention` 参数后，需要在多模态推理路径（Vision‑Language）跑完整的回归，尤其要检查 KV‑cache 对齐和 padding 是否一致。  
3. **Bias 初始化**：将 bias 从 `empty` 改为 `zeros`，确保模型加载后行为一致，若有模型迁移或 checkpoint 兼容性需求，确认不会导致数值偏差。  
4. **测试覆盖**：CI 新增的 VLM 测试仅跑 MMMU 0.2 的阈值，建议再加入对比 CPU/CUDA 结果的精度回归，以防出现 subtle 的数值差异。  
5. **文档与使用说明**：在 `README`/模型配置中标明 `attention-backend=ascend` 现在支持 vision‑instruct，并提示用户开启 `--disable-cuda-graph` 与 `--disable-radix-cache`（已在测试中使用）。  

总体而言，此次改动为 NPU 后端打开了跨模态大模型的可用性，但仍需关注实现的批处理效率与跨注意力的数值一致性。

---

### [BugFix] Fix server crashes when req.grammar and ngram spec are enabled (#17585)
**SHA**: `578b119` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/578b119bc66746d096a14917245af93c503674a1)

**🎯 变更类型**：Bug 修复 / 功能增强  
**⚡ 重要程度**：🟡 中（防止 server 在同时开启 `req.grammar` 与 n‑gram 时崩溃）  

**📋 变更摘要**  
1. 在 `NgramVerifyInput` 中新增 `grammar` 成员，并在构造函数中接受 `BaseGrammarObject`（默认 `None`）。  
2. `NgramWorker.forward_batch_generation` 增加对 `batch.has_grammar` 的分支：在目标验证阶段先把 CPU‑side 的 `retrive_next_*`、`draft_token` 拉回 CPU，调用新工具 `generate_token_bitmask` 生成结构化输出的 vocab mask，并在 `verify` 时把 mask 传进去。  
3. `spec_utils.generate_token_bitmask` 与内部 `traverse_tree`、`dfs` 逻辑加入 `vocab_size` 检查，防止越界访问；并将 `vocab_size` 通过参数向下传递。  

**🎯 影响范围**  
- `python/sglang/srt/speculative/ngram_info.py`、`ngram_worker.py`、`spec_utils.py`（核心推理路径）  
- `sglang.srt.constrained.base_grammar_backend.BaseGrammarObject`（新增依赖）  
- 可能影响 `ScheduleBatch`、`SpeculativeAlgorithm` 的调用方，需要确保 `spec_info.grammar` 正确填充。  

**💡 关注建议**  
1. **兼容性**：新增 `grammar` 参数默认 `None`，但 `verify` 仍会在 `batch.has_grammar` 为真时强制使用它。调用方在未开启 grammar 时务必保持 `batch.has_grammar=False`，否则可能出现 `None` 被解包的异常。  
2. **同步开销**：`retrive_next_token` 等张量从 GPU 拉回 CPU 可能导致额外的同步延迟，建议在性能基准中对比开启/关闭 grammar 的吞吐与 latency，评估是否需要缓存或异步拷贝。  
3. **内存/位图大小**：`allocate_token_bitmask` 仍按 32‑bit 打包，新增 `vocab_size` 检查防止越界，但若 vocab 超过 2^31‑1，位图大小可能不够，需在模型配置层面限制 vocab 范围或改写位图实现。  
4. **测试**：确认新增分支在无 grammar、仅 n‑gram、仅 grammar、两者均开启四种组合下都能通过现有单元/集成测试；特别检查 `batch.sampling_info.vocab_mask` 在每次 verify 前被清空的逻辑是否保持幂等。  
5. **文档**：更新 `speculative` 使用说明，明确在 `req.grammar` 与 `ngram` 同时开启时必须使用最新的服务器二进制，否则仍会出现旧版崩溃。  

整体而言，此次修改修复了致命的 server crash，逻辑清晰且对外保持向后兼容。但建议在高吞吐场景下评估 CPU‑GPU 数据搬迁带来的性能影响，并补充相应的回归测试与使用文档。

---

#### 🟢 低重要度变更 (16)

### [Fix] Revert back to using CUTLASS `mm_fp4` backend (#17369)
**SHA**: `ef134d4` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/ef134d407d6965c3e0ac3c35d3323a9a8ca20033)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `modelopt_quant.py` 中，修复 FP4 GEMM 后端的选择逻辑，若后端为自动模式则强制使用 `cutlass`，防止误用 FlashInfer。

---

### [Auto Sync] Update linear.py to assert shapes (20260130) (#17966)
**SHA**: `7412ceb` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/7412ceb4eb159c92841491075bde63735bac7c8d)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `linear.py` 中为权重加载函数添加 shape 校验断言，并提供错误信息，提升调试可读性。

---

### Add launch_command assignment in crash dump (#17967)
**SHA**: `0e18460` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/0e184609d329ecbb1fdda816cd164fd363fd4557)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `tokenizer_manager.py` 的崩溃转储中新增 `launch_command` 字段，记录启动时的完整命令行参数，以便调试。对功能无影响，仅提升可追溯性。

---

### [EPD][Perf] parallelize ZMQ send for encode server (#16487)
**SHA**: `a4df95c` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/a4df95c15f0f7fdfe170b78f601a30fa41d05f90)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `encode_server.py` 中引入同步 ZMQ 上下文和线程池，实现并行发送，使用 `run_in_executor` 将发送操作离线执行，提升编码服务的吞吐性能。

---

### Fix OOM in DeepSeek weight loading by deferring dict(weights) materialization (#17744)
**SHA**: `04efd03` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/04efd03dbf0f40c2c847e2dcaba84faa8dfcb128)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `deepseek_weight_loader.py` 中延迟 `dict(weights)` 的创建，仅在需要量化时才 materialize，避免不必要的内存占用，防止 OOM。

---

### Reduce topk kernel shared memory from 128KB to 32KB for better occupancy (#17747)
**SHA**: `45fe51a` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/45fe51a28e43c02a8aa7060a0b4ff06379926540)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：把 `topk` GPU kernel 的共享内存上限从 128 KB 降至 32 KB，并添加说明注释，以提升占用率和整体性能。

---

### add reasoning_tokens usage test for tool call (#18022)
**SHA**: `c72bf50` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/c72bf50706b351590c4c8b2a3d1e6ab710e76c06)

**🎯 变更类型**：测试修改  
**⚡ 重要程度**：🟢低  
**📋 摘要**：为工具调用测试新增 `test_reasoning_usage`，验证在开启 thinking 时返回的 `usage.reasoning_tokens` 大于 0，并在 DeepSeek 模型测试中启用该项。

---

### 【docs】【NPU】Update Expert Parallelism docs for Ascend NPU (#17940)
**SHA**: `c52578c` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/c52578c7fd19f64353513006b604b5050132901e)

**🎯 变更类型**：文档更新  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `docs/advanced_features/attention_backend.md` 与 `docs/advanced_features/expert_parallelism.md` 中新增并完善 Ascend NPU（华为昇腾）相关说明，更新支持矩阵、加入 EP/TP 与 Ascend FuseEP 的兼容性描述，以及提供 Ascend NPU 配置、DeepEP 参数和缓冲区大小的使用指南。

---

### Fix .gitignore may ignore files like core_attention.py (#18021)
**SHA**: `dc77def` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/dc77defdd0d4a1b94caaf8ffdd3c19080b3d751e)

**🎯 变更类型**：配置调整  
**⚡ 重要程度**：🟢低  
**📋 摘要**：将 `.gitignore` 中的 `core_*` 改为 `*.mudmp`，防止误忽略类似 `core_attention.py` 的文件，仅针对 MUSA 核心转储文件的忽略规则进行修正。

---

### Fix cuBLAS >=12.9 detection for cu12/cu13 package naming (#17766)
**SHA**: `d0d9cec` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/d0d9cecd1b34ead4caf506e0481011603ac87bcc)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：将 cuBLAS 版本检测函数重命名并改为同时检查 `nvidia-cublas` 与 `nvidia-cublas-cu12`，在相关模块中更新调用，修复 Cu12/Cu13 包命名导致的检测失效。

---

### [Diffusion] Fix FLUX.1-schnell time embedding argument mismatch (#17988)
**SHA**: `22aad4e` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/22aad4e2c4741b92a37d02e64502c710119cd098)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：修复 FLUX.1‑schnell 模型在时间嵌入的参数传递错误；仅在配置支持且提供 guidance 时才向 `time_text_embed` 传入 guidance 参数，避免参数不匹配导致运行时错误。

---

### Add ROCm + Mori docker build instructions in rocm.Dockerfile (#18018)
**SHA**: `ec76c39` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/ec76c390c95bbba7ad657e6c458772eda040425a)

**🎯 变更类型**：文档更新  
**⚡ 重要程度**：🟢低  
**📋 摘要**：在 `docker/rocm.Dockerfile` 中新增 ROCm + Mori 构建示例及使用说明，未改动实际构建逻辑。

---

### [NPU] fix sgl-kernel-npu package url error in npu.Dockerfile (#18017)
**SHA**: `ee3058c` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/ee3058c6e867e6cc0cfc6ff10bf318d476257180)

**🎯 变更类型**：配置调整  
**⚡ 重要程度**：🟢低  
**📋 摘要**：修正 `docker/npu.Dockerfile` 中 sgl‑kernel‑npu 包的下载 URL，改为使用 `-` 分隔并加入 `$(arch)`，避免原链接错误导致构建失败。

---

### doc update for CANN version (#18014)
**SHA**: `f6a4ff7` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/f6a4ff718f917d0c4076cc3787736ea9db1f4270)

**🎯 变更类型**：文档更新  
**⚡ 重要程度**：🟢 低  
**📋 摘要**：将 MindSpore 支持的 Ascend CANN 版本从 “8.3.RC2” 更新为 “8.5”，相应修改了下载说明文字。

---

### [sglang] fix mm token padded value overlap with text token id (#17781)
**SHA**: `5d00150` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/5d00150e9965f467399236ebf7819bbff5e385bb)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：引入 `MM_PAD_SHIFT_VALUE` 并通过 `_compute_pad_value` 为多模态 pad‑value 加偏移，使用 `lru_cache` 的 `sanity_check_mm_pad_shift_value` 在模型初始化时校验 vocab_size，防止 pad‑value 与文本 token ID 冲突。

---

### Optimize GDN decode for Qwen3 Next (#17094)
**SHA**: `81449b4` | 🔗 [查看提交](https://github.com/sgl-project/sglang/commit/81449b4beebf761eab1ad67ad19e3cab4c0109d4)

**🎯 变更类型**：代码重构  
**⚡ 重要程度**：🟢低  
**📋 摘要**：将 `fused_recurrent_gated_delta_rule_fwd` 中 BV 的上限由 8 提升至 32，以提升 Qwen3 Next 的 GDN 解码性能。

---

